# v1.6.7 — Self-healing отказ hh (2026-08-14)

- `discard_negotiation` больше НЕ хардкодит URL отказа: делает `get_negotiation` →
  берёт action `discard_by_employer` из `nego["actions"]` → бьёт по его `url`/`method`.
  hh сам отдаёт правильный URL → при смене эндпоинта hh поедем за ним без правок кода.
- Нет action discard_by_employer (закрытая вакансия / скрытое резюме / уже в отказе) →
  False (отказ невозможен), без хардкод-фолбэка. Контракт True/False/raise сохранён.
- Урок: не хардкодить hh-эндпоинты действий над откликом — брать из `actions[].url`.
