# v1.7.25 — Фикс харнесса merge-тестов (2026-08-31)

Тест-онли. Прод-код мержа (v1.7.24) не менялся.

- `test_candidate_merge.py`: `db_session.expire_all()` → `expunge_all()`. После `expire_all()` синхронное чтение `c1.id` в ассертах дёргало ленивый refresh (async-IO) вне greenlet → `MissingGreenlet`. `expunge_all()` отцепляет инстансы (значения PK сохраняются, читаются без IO), а `await db_session.get(...)` перечитывает свежее состояние из БД после мержа.
- Прогон на VPS: **10/10 зелёных** (8 мерж-тестов + 2 новых safety-теста из v1.7.24).
