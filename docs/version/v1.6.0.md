# v1.6.0 — «Переоценить всех» + фикс одиночной переоценки (2026-08-13)

- Кнопка «Переоценить всех» в меню «Действие» вакансии: обнуляет AI-оценки
  нетерминальных кандидатов → существующий крон `score_pending` постепенно
  переоценивает по НОВОМУ промту рубрики. `POST /vacancies/{id}/reevaluate`.
- Фикс бага: одиночная «Переоценить» (force) у уже оценённого кандидата возвращала
  СТАРЫЙ балл (partial-unique + не было delete). Теперь при force старая оценка
  удаляется перед пересчётом → балл реально обновляется.
