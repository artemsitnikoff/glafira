# v1.6.1 — Подтверждение «Переоценить всех»: вёрстка (2026-08-13)

- Кнопки «Да/Отмена» в подтверждении не влезали (две полноширинные стопкой + шапка +
  заметка). Переведены в компактный горизонтальный ряд + кап высоты/ширины попапа
  (`max-height`/`overflow-y`/`max-width`).
