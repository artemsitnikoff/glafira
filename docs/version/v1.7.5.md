# v1.7.5 — Talantix-бэкфилл: идемпотентность по навыкам (2026-08-15)

Мелкая подстраховка к джобе v1.7.4 `backfill_talantix_resumes`.

- Гард «не дублировать» проверял только наличие `CandidateExperience`. Кандидат с
  навыками, но БЕЗ опыта (skills-only) при **повторном** прогоне отбирался снова
  (нет опыта) и получал дубль навыков. Теперь гард проверяет опыт **ИЛИ** навыки —
  повторный запуск безопасен.
- На первый прогон не влияет (там ни опыта, ни навыков ещё нет). Первый прогон уже
  отработал: дозалито 5 547, реально пусто 9 314, ошибок 0.
