# v1.6.6 — Сквозная [reject]-трасса в hh-reject.log (2026-08-14)

- Часть шагов отказа писалась через `log_chat` → в ДРУГОЙ файл `chat.log`, а в
  `hh-reject.log` попадала только `logger.error` → по логу не было видно картины.
- Добавлена сквозная `[reject]`-трасса по каждому кандидату: старт → discard-результат
  (OK/уже в отказе/недоступен/не прошёл) → письмо (отправлено/почему нет). Логику не
  меняли, только логи.
