# v1.4.0 — Импорт комментариев работодателя с hh (2026-08-11)

- Заметки работодателя о резюме с hh импортируются в блок «Комментарии» карточки
  с пометкой «с hh» (read-only): разовый перенос + инкрементальный синк.
- Модель `Comment` +`source`/`external_id`/`author_name_ext`, `author_user_id` → nullable.
  Крон `poll_hh_comments` (*/15). Миграция `b9c0d1e2f3a4`.
- Попутно: справочник кронов `deploy/crontab.txt`, сверен с боевым.
