# v1.6.3 — Частичная попытка убрать greenlet-варнинг (2026-08-13)

- В `score_pending` добавлен dispose модульного engine (`app.database`) в finally.
- ⚠️ Оказалось НЕПОЛНЫМ: варнинг «non-checked-in connection / greenlet is being
  finalized» остался — соединение было взято и не возвращено, а не «висело в пуле».
  Настоящая причина найдена в v1.6.4.
