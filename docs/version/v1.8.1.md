# v1.8.1 — Бейдж «анонимная» в списке вакансий hh (2026-09-21)

Фронт-надстройка над v1.8.0 (импорт анонимных вакансий работодателя).

- В «Настройки → Интеграции → Показать вакансии с hh» у анонимных вакансий (`is_anonymous`) теперь виден бейдж **«анонимная»** (идиом `.conn-pill`, синий info-вид на токенах `--ark-blue-100`/`--accent`), с подсказкой «работодатель скрыт от соискателей, в публичном поиске её нет».
- Так рекрутёр сразу видит, какие из «новых» вакансий пришли из скрытого пула (через `/employers/{id}/vacancies/active`), а не из публичного поиска. Импорт/привязка — как у обычных.
- tsc/build чистые.
