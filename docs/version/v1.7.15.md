# v1.7.15 — Хабр: порядок ФИО «Имя Фамилия» (было перепутано) (2026-08-20)

После обогащения из профиля (v1.7.14) вскрылось: ФИО перепутаны — `first_name=Zdesenko,
last_name=Ilya` (должно Ilya Zdesenko). Хабр отдаёт имя в порядке **«Имя Фамилия»**
(проверено живьём: «Ilya Zdesenko», «Микаель Аветисян»), а маппер брал первое слово
как фамилию.

- `_habr_response_user_to_normalized`: `first_name=parts[0]`, `last_name=parts[1]`,
  `middle_name=parts[2]` (было наоборот).
- Тесты: мок-имена приведены к «Имя Фамилия»; guard `isinstance(profile, dict)` в
  импорте (моканный habr_client не ломает профиль-ветку — фолбэк на отклик).

Уже импортированным — разовый ре-синк перечитывает ФИО.
