// Встроенный вежливый текст отказа — зеркало backend build_polite_rejection_text()
// (app/services/integrations/hh/service.py). Используется как плейсхолдер/префилл,
// когда у компании (default_rejection_text) и у вакансии свой текст не задан.
//
// ⚠️ ЭТА КОНСТАНТА ПОПАДАЕТ В БД. Она префиллит textarea текста отказа
// (VacancyFormPage: value={formData.rejection_text ?? defaultRejectionText}) — стоит
// рекрутёру тронуть поле, и строка сохранится в rejection_text ДОСЛОВНО и уйдёт
// кандидату. Поэтому здесь только РЕАЛЬНЫЙ текст: никаких «<плейсхолдеров>».
//
// Соответствие беку: там build_polite_rejection_text(vacancy_name, company_name)
// добавляет в первую фразу вакансию и компанию-заказчика — но ТОЛЬКО когда рекрутёр
// свой текст НЕ задал (rejection_text = null). Задал свой — бек отдаёт его КАК ЕСТЬ,
// без подстановок. Текст ниже = зеркало обезличенной деградации бека
// (build_polite_rejection_text() без аргументов).
export const POLITE_REJECTION_FALLBACK =
  'Здравствуйте! Благодарим за интерес к нашей вакансии и время, уделённое отклику. ' +
  'К сожалению, по итогам рассмотрения мы приняли решение не продолжать общение по этой позиции. ' +
  'Это не оценка вас как специалиста — на данном этапе мы остановились на другой кандидатуре. ' +
  'Желаем успехов в поиске работы и будем рады видеть ваш отклик на наши будущие вакансии!';
