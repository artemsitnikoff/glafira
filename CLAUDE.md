# CLAUDE.md — ATS «Глафира Рекрутёр»

> Этот файл читается первым. Прежде чем что-то делать — прочитай его целиком.
> Продукт: ATS (система рекрутинга) с AI-агентом «Глафира» для российского рынка.
> Факты ниже сверены с реальным кодом (`grep`/`git`), а не взяты из отчётов.

---

## 0. КАК ЗДЕСЬ РАБОТАЮТ (прочитай обязательно)

Разработку ведут субагенты (`fastapi-expert` — бэкенд/миграции/seed; `react-expert` — фронт/CSS; `code-reviewer`) через одну консоль, **последовательно**. Архитектор/аналитик даёт планы и проверяет, агенты пишут код.

**ГЛАВНЫЙ УРОК ПРОЕКТА — агенты систематически выдают желаемое за действительное:**
- заглушки выдают за готовый функционал;
- пишут тесты «мимо поля» (тест проходит, а реальность не проверена) — и даже дёргают НЕсуществующие эндпоинты;
- галлюцинируют (выдуманные данные, кнопки с `console.log`, фейк-числа, несуществующие сущности);
- нарушают явные инструкции (например, добавляют запрещённые токены в `tokens.css`, лепят `alert()` вместо паттерна проекта);
- отчёты РЕГУЛЯРНО расходятся с реальным кодом.

**Рабочий принцип: ВЕРИТЬ `git diff` / `grep` / тестам, НЕ верить отчётам агента.**
Любое «функционал сохранён / всё работает» в отчёте проверять руками: `grep` по ключевым словам, чтение диффа, запуск тестов, ручная проверка в браузере на VPS. За проект это спасало на каждом шаге (последний пример: агент написал тесты против несуществующего `POST /applications`; другой повесил rename-PATCH на каждый keystroke и насовал `alert()`).

**Субагентам РАЗРЕШЕНО коммитить/пушить (с 2026-06-18, явное указание заказчика: «делай субагентами, пусть пушат»).** Реализацию вести СУБАГЕНТАМИ (fastapi-expert/react-expert), они сами ведут полный цикл (правка→коммит→`git push`→автодеплой). Запрет git-операций в промптах НЕ ставить. Защита смещена на **ПОСТ-ФАКТУМ проверку оркестратором** после КАЖДОГО субагента (раньше она была подстраховкой к запрету — теперь это основной контроль): `git log`/`git diff` против последнего своего коммита (что реально ушло?), `gh run list` (деплой зелёный?), одна alembic-голова?, `grep` фейков/`console`/выдуманных токенов/локальных импортов, прогон `tsc`/`build`/`py_compile`, чтение диффа на утечку `company_id`/PII, финальный `code-reviewer` → **фикс-форвард новым коммитом**, если что-то не так. ⚠️ Миграции данных на проде всё равно сверять (идемпотентность/NULL), но пуш субагента не блокировать. Память `subagents-no-git-operations`.

**Принцип «никаких фейк-заглушек»:** функционал либо работает по-настоящему, либо честно помечен «в разработке/скоро» (disabled-контролы, видно что неактивно). Недопустимо: кнопка выглядит рабочей, но ничего не делает; UI редактируется, но изменения не сохраняются; поле принимается схемой, но молча теряется. Молчаливый обман в HR-решениях (скоринг, отказы, верификация) — особенно критичен.

**Токены CSS:** использовать ТОЛЬКО реально существующие переменные из `frontend/src/styles/tokens.css`. НЕ выдумывать «семантические» имена (`--bg-hover`, `--accent-danger`, `--fg-link`, `--border-strong` — таких НЕТ, проверено). Если нужного токена нет — СТОП, спросить, не добавлять самовольно. Известные подмены: `--bg-hover`→`--bg-3-hover`, `--border-strong`→`--ark-gray-400`, `--fg-link`→`--accent`, `--dur-base`→`--dur-normal`, `--shadow-2`/`--shadow-3`→`--shadow-lg`, `--rad-pill`/`--rad-md`→`--radius-full`/`--radius-md`, `--accent-soft`→`--ark-blue-100`, `--ark-violet-600`→`--ark-violet-500` (`#7E5CF0`). ⚠️ Отдельно — дрейф палитры: у СУЩЕСТВУЮЩЕГО `--ark-violet-100` эталон подставляет фоллбэк `#EEEAFE`, а реальный токен `#ECE7FE` — брать токен, фоллбэк эталона не копировать. (⚠️ react-expert не раз «галлюцинировал» `--dur-base` как существующий — проверять КАЖДЫЙ `var(--…)` из диффа грепом по tokens.css.)

⚠️ **В самих `frontend/src` есть ПРЕЖНИЕ битые токены** (регресс мимо этого правила, все закоммичены давно; перечислены только те, что реально НЕ резолвятся — БЕЗ фоллбэка): `--border-strong` в `VacancyForm.css:833,952`; `--shadow-2` в `Pulse.css:197`; `--fg-on-accent` в `Button.css:44,63` И `LoginPage.css:87`; `--status-error-bg` в `LoginPage.css:72`. Тронешь эти файлы — почини заодно. (НЕ баги, не трогать: `--status-error-border` `LoginPage.css:78` и `--status-success-hover` `Button.css:67` — фантомы, но с фоллбэком на существующий токен (`var(--status-error)`/`var(--status-success)`) → резолвятся, это корректный defensive-паттерн. Ложные срабатывания скана: `--stage-color`/`--ch-color`/`--cp-color` задаются инлайн-стилем в рантайме, `--profile-rail-width` определён в `Funnel.css`.)

---

## 1. СТЕК

**Бэкенд:** FastAPI + PostgreSQL 16 + SQLAlchemy 2.0 (async) + Alembic + Pydantic v2 + asyncpg + JWT (python-jose) + pytest (+pytest-asyncio).

**Фронтенд:** React 18.3 + TypeScript 5.7 (strict) + Vite 5 + React Router v6 + TanStack Query v5 + Zustand 4 + axios + recharts + lucide-react + **голый CSS (токены, НЕ Tailwind)**.

**Монорепо:** `/backend` + `/frontend` + `/contracts` (+ `/docs`, `/project`).

**LLM:** через **OpenRouter** (httpx → `{OPENROUTER_BASE_URL}/chat/completions`, OpenAI-формат), модель `anthropic/claude-sonnet-4-6` (`settings.GLAFIRA_MODEL`). Клиент `backend/app/services/glafira/client.py` (методы `call_json` / `call_text`; пустой `OPENROUTER_API_KEY` → не делает живых вызовов). НЕ прямой Anthropic SDK — именно OpenRouter. (Переменная `ANTHROPIC_API_KEY` в конфиге ещё есть, но путь скоринга идёт через OpenRouter.)

> **ИСКЛЮЧЕНИЕ — интернет-разведка верификации (OSINT)** идёт НЕ через OpenRouter, а через **claude CLI с WebSearch/WebFetch** (subprocess, `services/glafira/claude_cli.py`, по образцу ArkadyJarvis). Образ бека (`backend/Dockerfile`) ставит Node 20 + `@anthropic-ai/claude-code`. Токен — из общего файла `CLAUDE_TOKEN_FILE` (формат `{access_token,…}`, держит свежим ArkadyJarvis; мы только читаем, НЕ рефрешим) или из `CLAUDE_CODE_OAUTH_TOKEN`. Подробно — память `verification-osint-claude-cli`.

**VPS:** домен `glafira.dclouds.ru`, Docker compose (postgres + бек + фронт), HTTPS (certbot), nginx **общий** (на сервере много чужих сервисов — их не трогать). Путь `/var/www/glafira`. **Push в `main` = автодеплой** (см. §6).

**Доступы:** demo-админ создаётся в `app/seed.py`; секреты — в `.env` на VPS (НЕ в git). Конкретные значения в доках НЕ хранить.

---

## 2. АРХИТЕКТУРНЫЕ ИНВАРИАНТЫ (не нарушать)

1. **Верификация без подписанного согласия (ПдН/152-ФЗ) → 403 `CONSENT_REQUIRED`.** Доступ к проверке только после подписи. Согласие даёт либо кандидат (online-подписание), либо рекрутёр под свою ответственность кнопкой **«ПдН подписан»** (`POST /consent/confirm-signed`, бумага и т.п.) — обе кнопки в блоке согласия таба Верификация.
2. **Каждое изменяющее действие → запись в `audit_log`.** Действия AI — с `actor_type='ai'`. (Отдельно: лента «Все действия» на карточке читает таблицу `Event`, не `audit_log` — новый тип события требует расширения CHECK `Event.type` + рендера.)
3. **Все записи с `company_id`** (мультитенантность).
4. **Скоринг / AI → строгий JSON.** При сбое парсинга — 502 `GLAFIRA_PARSE_ERROR`, НЕ молчаливый фейк (НЕ хардкод score=50, НЕ поддельная реплика).
5. **Переход `application → hired` идемпотентно создаёт `Employee` + план адаптации** (передача в раздел Пульс). Инвариант проверяется в seed реальным `move_application`.
6. **Единый формат ошибок** `{error: {code, message, details}}`. Бизнес-ошибки — через `core/errors.py` (`AppError` + наследники), НЕ сырой `HTTPException`.
7. **Правило 400 vs 422:** бизнес-ошибки → 400 (`ValidationError`/`ConflictError`=409/`NotFoundError`=404 и т.д.); ошибки валидации формы (Pydantic) → 422.
8. **Верификация — частично РЕАЛЬНАЯ** (с ~2026-06-04, `services/glafira/verify.py`): блоки — **контакты** (телефон/email/ФИО через DaData Clean API — реально), **госреестры** (ФССП/ФНС/банкротство/санкции/алименты — честные заглушки «Не подключено», 152-ФЗ, БЕЗ фейк-вердиктов), **OSINT** («Публичная экспертиза» + «Упоминания» — реальный веб-поиск через claude CLI, идёт В ФОНЕ `fill_candidate_osint` + поллинг на фронте). `is_mock=False`. Реальной интеграции с госреестрами НЕТ — заглушки НЕ включать как вердикты до неё (мина 152-ФЗ). `GLAFIRA_VERIFY_MODE` — legacy (verify_candidate на него не смотрит). В поиск идут ФИО+город+должность+компания+год+email+телефон (по согласию); плашка под блоками описывает это честно — НЕ менять текст плашки, не сверив с тем, что реально шлётся.
9. **Биллинг (с v0.9.48):** компания с `paid_until` в прошлом ИЛИ NULL → все аутентифицированные запросы её юзеров отбиваются **402 `SUBSCRIPTION_EXPIRED`** (гейт в `app/deps.py::get_current_user` через `company_subscription_active`, scoped по `user.company_id` — тариф А не влияет на Б). NULL = заблокирована (fail-closed). Логин и `/auth/refresh` НЕ гейтятся (фронт ловит 402 и показывает экран). **Суперадмин-сервис НЕ гейтится** — владелец заходит всегда. Дата `paid_until` правится руками в суперадминке. Память `billing-paid-until-state`.

---

## 3. БЕКЕНД (состояние)

Завершён по основной функциональности. **278 эндпоинтов** в 28 роутер-модулях (`app/api/v1/` — 30 `.py` всего: 28 модулей + `router.py`-агрегатор с 30 `include_router` + `__init__.py`; включений 30, а модулей 28, потому что ДВА модуля экспортируют по два роутера: `glafira.py` — `router` + `candidates_evaluation_router`, `integrations.py` — `router` + `public_router` (публичные OAuth-callback'и без auth-гарда, вынесены в v1.0.8)). Тесты в `tests/` — **92 файла, 1238 тестов** (offline — LLM-ключ пуст, живые вызовы не идут). (Числа растут с каждой фичей — при сомнении пересчитать `grep`, не верить цифре вслепую. ⚠️ Греп эндпоинтов по `@router\.` пропустит `candidates_evaluation_router`/`public_router` — ловить `@[a-z_]*router\.`. Последняя сверка CLAUDE.md с кодом — 2026-07-24, версия 1.1.12.)

> ⚠️ **С v1.1.3 добавлено 30 тестов (1203 → 1233), полным прогоном они НЕ подтверждены** (v1.1.4: 7 в `TestAuthorUserLink`; v1.1.6: 2 на audit-запись привязки + 6 в `TestManagerNotifications`; v1.1.8: 14 в `test_offer.py`; **v1.1.9: +1 `test_auto_move_target_can_be_offer` + правка `test_delete_protected_stage_fails`**). Локально их запустить нечем — прогон на VPS за заказчиком: `docker compose -f docker-compose.prod.yml run --rm backend pytest tests/test_hiring_requests.py tests/test_offer.py tests/test_automation.py tests/test_vacancy_stages.py -q`.
>
> ✅ **Последний полный прогон pytest (VPS, 2026-07-20, v1.1.3): 1203/1203 ЗЕЛЁНЫЙ, подтверждён заказчиком (8 мин).** Прогон после эры «отмена/перенос интервью» дал 1202 passed + 1 failed; единственное падение — протухший тест, НЕ баг продукта (разбор ниже в «Свежие фичи 1.0.11–1.1.3»). ТРИ раунда чистки тест-долга: раунд 1 (713, v0.9.25-29 — 90 падений→0, 9 реальных прод-багов); раунд 2 (1103, v0.9.144 — security-эра 0.9.127-143, 24 падения, ВСЕ протухшие, 0 багов); **раунд 3 (1188, v1.0.8-1.0.10 — после эр «обязательный заказчик» + модуль заявок: 81 failed + 1 error → 3 РЕАЛЬНЫХ бага прод/инфры + ~74 протухших теста).** Раунд 3 — единственный, где всплыли реальные регрессы (см. «Свежие фичи 1.0.x» + память `full-suite-test-debt`). **Урок: гонять ВЕСЬ suite после ЛЮБОЙ эры со сменой контракта (обязательное поле / новый роутер-гард) — оставляет и протухшие тесты, И иногда реальный регресс** + при починке теста сверять ВЕСЬ его body (httpx `.json()`/`.status_code` СИНХРОННЫ → `MagicMock`, не `AsyncMock`). ⚠️ **Грабля identity-map (укусила ДВАЖДЫ в раунде 3):** сессия с `expire_on_commit=False` (и прод `app/database.py`, и тест-conftest) → после `commit()` повторное чтение той же сущности в том же запросе/тесте отдаёт ТОТ ЖЕ инстанс со stale-полями (`selectinload`/`get` не перечитывают загруженное); DB-изменения через триггер/`ondelete`/сырой SQL в ORM-инстанс НЕ попадают. Лечить: создавать связанное через relationship (не `_id=`), либо `refresh()`/`expire()`/`populate_existing` перед повторным чтением. Память `full-suite-test-debt`.
>
> ⚠️ **ВАКУУМНЫЙ ТЕСТ — зелёный не потому, что инвариант держится (вскрыто 2026-07-20).** `test_calculate_slots_all_participants_must_be_free` числился рабочим, а на деле не проверял НИЧЕГО: мок занятости Б24 шёл с ключами `FROM`/`TO`, которых парсер `_parse_b24_accessibility` не знает (`interview_slots.py:91-92` читает ТОЛЬКО `DATE_FROM`/`DATE_TO`, иначе `continue`) → занятость была невидима. Держался он на ВТОРОМ дефекте: `now()` не заморожен при `horizon_days=1`, и в выходной горизонт упирался в нерабочий день → пустой список → `assert slots == []` проходил по неверной причине. Два дефекта маскировали друг друга больше месяца; вскрылось, лишь когда календарь сдвинул горизонт на рабочий день. **Уроки: (1) тест на «пусто» обязан доказывать, что пусто ИМЕННО из-за инварианта — иначе он вечнозелёный; (2) любой расчёт от `now()` в тесте морозить (идиом `_FrozenNow` + патч `interview_slots.datetime`); (3) при починке теста сверять, что мок вообще ДОХОДИТ до парсера — совпадение ключей проверять грепом по парсеру, а не по памяти.**
> ✅ **Глубокий бэкенд-аудит пройден** (эра 0.9.1x): 0 Critical, все High/Medium закрыты, остался только Low (defense-in-depth `company_id`). Память `backend-deep-audit-0-9`.

> ⚠️ **`contracts/openapi.json` ПРОТУХ РАДИКАЛЬНО** (сверено 2026-07-15): не менялся с 2026-05-28 (коммит `050cd77`, **571 коммит назад**), содержит **79 путей / 102 операции против 257 реальных роутов** — покрыто ~40% API (⚠️ сравнивать сопоставимое: 79 — это URL-пути, а не method-level роуты). Всё, что появилось с тех пор (этапы воронки, smart/auto, интервью, hh/habr/avito/mango, auto_qa, rejection_text, П.5 и т.д.), подключено на фронте через **локальные TS-типы + `as`-cast** — сейчас это **49 строк-маркеров в 35 файлах** (`grep -rE "openapi (не реген|отстаёт|ещё)" frontend/src`), честно помеченных комментариями (`api/aliases.ts`, `api/mutations/vacancies.ts`, `api/hooks/useSmartSearch.ts`, `pages/VacancyFormPage.tsx` и др.). Обмана нет (расхождение задокументировано в коде), но техдолг по 10+ модулям. Регенерировать против поднятого бэка, когда появится возможность, и тогда убрать cast.

**Модели/таблицы (53, `grep __tablename__`):** companies (+ `paid_until` Date nullable — биллинг, v0.9.48, см. инв. §2.9), users, clients, vacancies, vacancy_stages, vacancy_team, candidates, candidate_experience, candidate_skills, candidate_education, applications, stage_history, ai_evaluations, consents, documents, messages, comments, employees, pulse_surveys, pulse_plan_items, pulse_alerts, survey_templates, audit_log, events, email_templates, verifications, reject_reasons, tags, candidate_tags, glafira_settings, integrations, hh_integrations, hh_oauth_states, smart_search_runs, funnel_templates, funnel_template_stages, company_default_stages, candidate_import_jobs (импорт из Excel), base_search_runs (история поиска по своей базе), candidate_embeddings (pgvector-эмбеддинги резюме, заход B), **message_templates** (шаблоны быстрых сообщений для чата, v0.9.30 — общие на компанию, см. память `message-templates-state`), **calls** + **call_sync_jobs** (звонки Mango Office + джоб выгрузки истории, v0.9.34 — см. память `mango-calls-integration-state`), **habr_integrations** + **habr_oauth_states** (Хабр Карьера OAuth + синк откликов, v0.9.112-122 — память `habr-oauth-scaffold`), **avito_integrations** (Авито Работа отклики, v0.9.123-126 — память `avito-job-responses-sync`), **auto_searches** + **auto_search_runs** (Автоподбор — сохранённые автопоиски резюме hh + прогоны AI-оценки, v0.9.151-155 — память `autopodbor-feature-state`), **interview_links** (запись на интервью через календарь Б24: token/status/expires_at/slot_from/slot_to/b24_event_id, v0.9.221 — память `interview-scheduling-b24-state`), **hiring_requests** + **request_comments** + **request_funnel_stages** + **request_settings** (модуль «Заявки на подбор» — заявка/тред/кастомные этапы воронки заявок/настройки-тумблеры+токен формы, v0.9.239-241 — память `hiring-requests-module-state`).

**Источник кандидата (`Candidate.source` — ЕДИНСТВЕННЫЙ источник; `Application.source` был мёртвым дублём, удалён v0.9.109, память `candidate-source-model`):** hh, avito, superjob, telegram, referral, direct, agency, import, manual, linkedin, **potok**, **habr**, other. `'potok'` добавлен миграцией `j1k2l3m4n5o6`, `'habr'` — позже (Хабр Карьера). При добавлении нового источника обновлять И значение в модели, И миграцию, И `CandidateSource` Literal в `schemas/candidate.py`, И `lib/source-colors.ts`/фильтры на фронте. Текущая голова alembic — **`b3c4d5e6f7a8`** (`b3c4d5e6f7a8_add_interview_reschedule_count.py`, v1.1.0); всего **74 ревизии**, ОДНА голова, корень `824a6e6b9004`, цепочка целая (0 висячих `down_revision`) — пересчитано по графу 2026-07-20. Перед новой миграцией найти НАСТОЯЩУЮ голову — и НЕ доверять ни этой строке, ни докстрингам миграций (память `alembic-find-true-head`). ⚠️ Грабли поиска, все проверены на этом репо: (1) ревизии в РАЗНЫХ стилях — `down_revision = 'x'`, `down_revision: str = 'x'`, `down_revision: Union[str, None] = "x"` — греп обязан ловить все; (2) **парсить ПОСТРОЧНО**: в докстрингах есть строки вида `down_revision: a9b0c1d2e3f4 (единственная голова)` БЕЗ `=` — многострочный регекс с `[^=]+` перепрыгивает их и цепляет чужое присваивание, давая ФАНТОМНЫЕ головы (реальный инцидент: показало 3 головы вместо 1); (3) докстринги `b1c2d3e4f5a6`/`z8a1b2c3d4e5` объявляют «головой» свой `down_revision` — ревизии из 2026-06, головами давно НЕ являющиеся; (4) ⚠️ **КОЛЛИЗИЯ revision id** — перед созданием миграции ГРЕПНУТЬ выбранный id по `alembic/versions/*.py`: реальный инцидент 2026-07-18 — взял `a1b2c3d4e5f6`, а он уже занят `add_hh_chat_id` (2 файла с одним revision → граф ломается, автодеплой падает). Голову брать только пересчётом по графу, id новой миграции — проверять на уникальность.

**Этапы воронки (`STAGES`, 9 шт., hex-цвета — источник правды на беке** `core/stages.py`, цвет вычисляется, НЕ хранится в БД):
- `response` Отклик, `added` Добавлен (system), `selected` Отобран, `recruiter` Контакт с рекрутером, `interview` Интервью, `manager` Контакт с менеджером, `offer` Оффер, `hired` Нанят (terminal), `rejected` Отказ (terminal).

**Защищённые этапы (`PROTECTED_STAGE_KEYS` в `core/stages.py`):** `hired`, `rejected`, `added`, `response`, **`offer`** (добавлен v1.1.9) — нельзя удалять/менять `stage_key` (завязаны на найм, отказ, старт, fallback; `offer` — на отправку оффера, удаляемый этап оставил бы фичу без триггера). Защита **серверная** (в 3 точках CRUD этапов: `vacancy.py` / `funnel_templates.py` / `default_funnel.py`; UI-защиты недостаточно). ⚠️ **`PROTECTED_STAGE_KEYS` ≠ цели автоперевода:** скоринг (`resolve_auto_target_stage`) исключает цели авто-перевода по ОТДЕЛЬНОЙ константе `AUTO_MOVE_EXCLUDED_STAGE_KEYS` (= `{hired,rejected,added,response}`, БЕЗ `offer`) — авто-перевод в «Оффер» остаётся допустимым, добавление `offer` в PROTECTED его НЕ запретило (развязано в v1.1.9, иначе молча сломался бы автоперевод). Фронт: `offer` защищён от УДАЛЕНИЯ в обоих редакторах воронки (`VacancyFormPage`/`SettingsFunnel`), но остаётся обычным «средним» этапом — переименование/порядок разрешены (в отличие от `hired` — не мислейблим в «Системный»), т.к. конфликт был именно про удаление stage_key.

**Связь этап↔кандидат — строковая** (`Application.stage == VacancyStage.stage_key`, без FK; `stage_history.from/to_stage` — тоже по строке). Поэтому:
- переименование этапа меняет ТОЛЬКО `label`, НИКОГДА `stage_key` (иначе осиротит applications + stage_history);
- смена порядка этапов = только `order_index` (история переходов по `stage_key` остаётся, аналитика не ломается);
- CRUD этапов живой вакансии — гранулярные эндпоинты (`POST/PATCH/DELETE/PUT /vacancies/{id}/stages[...]`), `update_vacancy` этапы НЕ трогает. Каждое действие = свой запрос (массив целиком слать нельзя — rename станет неотличим от delete+add).

**Описание этапа (`description`, с ~2026-06-04):** свободный текст-инструкция для команды (что делать на этапе, суть тестового, чек-лист). Колонка `description` (Text, nullable) в `vacancy_stages` + `company_default_stages` + `funnel_template_stages` (миграция `f3a4b5c6d7e8`). Редактируется в форме вакансии (FunnelStep, по blur PATCH) и в Настройках → «Воронка по умолчанию». PATCH этапа теперь меняет `label` И/ИЛИ `description` (через `model_fields_set` — label-only PATCH не затирает описание). На `stage_key`/порядок/аналитику не влияет.

**LLM-сервисы:** `services/glafira/` — scoring (скоринг резюме, взвешенная 100-балльная рубрика; в промпт `description` вакансии идёт со снятыми HTML-тегами `_strip_html`), screening (диалог-скрининг), resume_parse (парсинг PDF/TXT — DOC/DOCX/RTF НЕ парсятся), verify (см. инв. §2.8 — контакты DaData + госреестр-заглушки + OSINT через claude CLI, consent-gated, OSINT в фоне), employee_summary (AI-сводка сотрудника). Все с anti-hallucination guards в промптах и строгим JSON-контрактом. Ретрай-backoff на 403/429/5xx в `client.py` (OpenRouter); claude CLI (`claude_cli.py`) — graceful→None при любом сбое.

**Умный подбор — ДВЕ ветки (развилка источника, см. память `smart-search-state`):**
- **Ветка А — hh** (`services/smart_search.py` + `api/v1/smart.py`): активный сорсинг резюме hh — фоновая `asyncio`-задача search→eval→invite (паттерн OSINT). Модель `SmartSearchRun` (`smart_search_runs`). Оценка — `scoring.score_resume_dict` (рубрика, БЕЗ персиста). Эндпоинты `/smart/access|vacancies|search|runs[/{id}]|vacancy-filters/{id}`. **Денежные предохранители FAIL-CLOSED:** приглашения только при `has_paid_access && vacancy.hh_vacancy_id`; квота hh не определена/мала → 400; капы scan/invite. ⚠️ ВРЕМЕННЫЕ диаг-логи `[smart] payable_api_actions raw=`/`search_params=`. Тесты на моках. ПЛАТНЫЙ цикл подтверждён заказчиком.
- **Ветка Б — по СВОЕЙ базе** (`services/base_search.py`; заход A с v0.7.0, заход B/семантика с v0.8.0, гибрид с v0.9.0): **ГИБРИД retrieve→rerank**.
  - **Retrieve (мгновенно): семантический косинус по ВСЕЙ базе** через **pgvector HNSW** (`candidate_embeddings`, `SET LOCAL hnsw.ef_search`). Эмбеддинги — **локальная ONNX-модель fastembed** `paraphrase-multilingual-MiniLM-L12-v2` (384-dim), `services/embeddings.py` (single-flight синглтон `threading.Lock`, прогрев в lifespan, build внутри `to_thread`; fastembed-импорт на модуле с graceful-фолбэком). Переиндексация `reindex_all_embeddings` — фоновая задача (возвращает task), запуск из **Настройки→AI**. `match_percent` семантики = cosine→percent. Фолбэк, если эмбеддингов нет/вектор недоступен — SQL top-N (заход A не ломаем).
  - **Заход A (SQL, оставлен как фолбэк/основа фильтров):** `parse_query_to_criteria` (call_json → {role,skills,city,salary} + фолбэк на ключевые слова, НЕ 500); `search_base` (ILIKE last_position/city + EXISTS candidate_skills scoped-company + ЗП BETWEEN NULL-инклюзив; `match_percent` = РЕАЛЬНЫЙ overlap навыков; **фильтр `tags`/`vacancy_id` FAIL-CLOSED** на полностью невалидных UUID → `false()`, не вся база); `search_by_vacancy` (переиспользует `derive_vacancy_filters`, принимает override-критерии от фронта).
  - **Rerank (по кнопке, тратит AI-токены):** `_run_base_evaluate` — AI-оценка топ-N (`score_resume_dict`) кандидатов из retrieve, фоновая `asyncio`-задача. Запуск через `POST /smart/base/runs/{id}/evaluate` с **TOCTOU-предохранителем** (атомарный conditional UPDATE на сессии запроса → 409 `ConflictError` при `status='running'`; ValidationError для прочих невалидных статусов).
  - Эндпоинты `/smart/base/{search,runs,count,runs/{id}/evaluate,runs/{id}/mark-added}` (RBAC manager-forbidden). История `base_search_runs`. Опыт хранится строками → мягкий фильтр. «+ В вакансию» переиспользует `assign_candidate_to_vacancy` (невалидный/чужой этап → мягкий фолбэк на 1-й реальный этап, garbage не в `STAGES` → 400). Первый реальный поиск на живой базе — заказчик.

**Импорт кандидатов (бек, `services/candidate_import.py` + `api/v1/candidate_import.py`, см. память `candidate-import-excel-state`):** два источника в ОБЩУЮ базу (БЕЗ вакансии/этапа). (1) **Из файла Excel** (.xlsx/.xls): `/candidates/import/{parse,preview,execute,jobs/{id}}` — openpyxl через `to_thread`, авто-распознавание колонок, очистка (ФИО-коды/телефоны→+7), дедуп company-scoped (телефон-варианты + lower(email)), async-джоб `CandidateImportJob` (`candidate_import_jobs`: батчи 500, короткие сессии, GC-защита `_active_tasks`+add_done_callback, наивный finished_at). (2) **Из Поток (potok.io) по API** (с v0.6.8, `services/integrations/potok/`): офиц. API v3 (`https://app.potok.io/api/v3`, Bearer-токен компании), `/candidates/import/potok/{preview,execute}` → `GET /applicants.json` пагинацией → маппинг резюме (resume под `resumes[].cv_params`: experience→candidate_experience, skill_set→candidate_skills, education→candidate_education, languages→`extra.languages`, about→resume_summary), source='potok'. **Токен НЕ персистится** (только в памяти задачи, не в БД/логах/ответах). RBAC manager-forbidden, company_id из контекста, каждая строка в savepoint (сбой одной не валит батч). ⚠️ Живой API Поток из dev-среды НЕ проверен (гео/IP) — маппер по OpenAPI-спеке, пинит заказчик.

**⚠️ ВСЕ ПИСЬМА — ТОЛЬКО ЧЕРЕЗ БРЕНДИРОВАННЫЙ ШАБЛОН ГЛАФИРЫ.** Любой email, который система шлёт наружу (кандидату/пользователю), формируется через `services/integrations/smtp/templates.py` — универсальный `render_simple_email(heading, body_html, preheader)` (внутри `_email_shell` — фирменный враппер с шапкой/кнопкой) ИЛИ специализированный `render_credentials_email`. **НИКОГДА не слать голый `body_text`-plaintext как единственное тело.** Паттерн: собрать `body_html` через `render_simple_email(...)` и передать `body_html=` в `send_email(...)` (при этом `body_text` оставить как плоский фолбэк для клиентов без HTML). Кнопка — идиом `<td style="border-radius:8px;background:#2A8AF0;"><a …>`. `body_html` формируется КОДОМ (не пользовательский ввод) — любые вставки экранировать `html.escape`. Заказчик требовал это явно (2026-07: письмо-приглашение на интервью ушло plaintext — переделано на шаблон). При добавлении нового письма СНАЧАЛА посмотреть существующие рендеры, не изобретать своё.

**Email «Доступ к аккаунту» (credentials, реален с v0.2.49):** `services/integrations/smtp/templates.py::render_credentials_email` (HTML из кода, не из БД/файла) → `service.py::send_credentials_email` → `send_email` (stdlib `smtplib`, SMTP компании). Триггеры: создание юзера (`POST /users`) и импорт из Б24 — оба шлют письмо с логином/паролем, если SMTP настроен. Фронт (`CreateUserModal`/`BitrixImportModal`) обрабатывает и успех, и fallback. Прототип письма — `project/Письмо - Доступ к аккаунту.html`. Письмо-приглашение на интервью (`services/glafira/interview_schedule.py`) — тоже через `render_simple_email` (v0.9.227).

**Снятые CHECK-констрейнты (миграции):** `check_stage_key` на `vacancy_stages` (`a1b2c3d4e5f7`) и `check_application_stage` на `applications.stage` (`c8d9e0f1a2b3`) сняты — чтобы воронка могла иметь кастомные этапы и кандидаты могли на них перемещаться. Downgrade восстанавливает прежний список.

**seed:**
- `python -m app.seed` — базовый (компания, админ, справочник причин отказа, дефолтные настройки). Для чистого прода. Идемпотентен (exists-guard).
- `python -m app.seed_demo` — демо-данные. **НЕ мягко-идемпотентен:** в начале `cleanup_demo` УДАЛЯЕТ свои demo-сущности (кандидаты с `extra->>'demo'='true'`; вакансии/клиенты ПО ИМЕНИ из списков + каскад) и пересоздаёт. Реальные данные целы, ПОКА имена не пересекаются с demo. Всё в одной транзакции. Создаёт клиентов, вакансии + vacancy_stages, кандидатов по этапам, employee (через реальный `move→hired`), pulse-опросы, consent, документы, сообщения, AI-оценки. `ai_score` проставляется напрямую (LLM НЕ вызывается). `stage_history` монотонный с реалистичными интервалами.
- `python -m app.jobs.regenerate_employee_summaries` — батч AI-сводок сотрудников (cron на VPS).

---

## 4. ФРОНТЕНД — ЭКРАНЫ

15 экранов (компоненты в `frontend/src/`). Подробные ТЗ по экранам — в `docs/tz/` + `project/docs/` (экран 11 — `project/docs/11 - Умный подбор.md` + `11b - Автоподбор.md`, экран 12 — `project/docs/12 - Импорт кандидатов.md`, редизайн таба Резюме — `04a - Резюме - редизайн таба.md`, экран 15 «Заявки на подбор» — эталон `project/components/Requests.jsx` + `styles/requests.css` + `Форма заявки - менеджер.html`).

1. **Главная** (Home/Dashboard) — KPI-сетка, «Требуют внимания», «Лента событий» (polling, live-dot), блок «Адаптация/Пульс», «Топ-источники».
2. **Вакансии** — список в сайдбаре (inline), форма создания (4 шага, кастомные этапы), форма добавления кандидата (full-screen), архив.
3. **Воронка** (`/vacancies/:id`) — таблица кандидатов (закреплённый профиль-рельс 378px + скролл-часть), чипы этапов, сортировка, фильтры (drawer), bulk-действия, выезд карточки (detailMode — оверлей `left:378px`).
4. **Карточка соискателя** — right-side панель (оверлей в воронке), 8 табов: Резюме, Оценка AI, Верификация (ПдН-gated), Чат, **Звонки** (Mango, v0.9.34), Документы, Комментарии, Все действия. Тулбар — строго 5 кнопок: Перевести / Отклонить / Комментарий / ПдН / ✕.
5. **Кандидаты — общая база** (CandidatesPool) — сетка карточек всех кандидатов + карточка-пул (та же карточка, режим `fromPool`).
6. **Аналитика** — 7 отчётов (Обзор, Скорость, Воронка, Источники, Отказы, Текучка, Рекрутёры).
7. **Пульс — главный** — пост-найм-модуль, таблица сотрудников в адаптации, алерты, риск ухода.
8. **Пульс — карточка сотрудника** — профиль адаптации, план, опросы, AI-сводка.
9. **Настройки** — профиль, команда, интеграции, голос Глафиры, шаблоны, «Воронка по умолчанию» (шаблон для новых вакансий), биллинг.
10. **Сайдбар** — постоянная навигация, раскрытие подменю (Вакансии/Аналитика inline), бейдж Пульса, user-card. **Список вакансий в сайдбаре — эталонный, НЕ трогать.** Пункт ✨ «Умный подбор» (beta) — сразу после «Кандидаты».
11. **Умный подбор** (`/smart`, beta, RoleGuard admin/recruiter) — **развилка источника из ТРЁХ карточек** (`SSFork`, in-state `mode`). ⚠️ **Открытый визуальный баг:** карточек три (`.ssf-hh`/`.ssf-base`/`.ssf-auto`), а сетка в коде осталась двухколоночной — `.ss-page .ssf-grid { grid-template-columns: 1fr 1fr; gap:16px }` (`frontend/src/pages/smart/smart-search.css:1423`, файл не трогали с v0.9.107, ДО Автоподбора) → третья карточка уезжает на второй ряд; в эталоне `project/styles/smart-search.css:691` — `repeat(3, minmax(0,1fr))`, `gap:14px`, и брейкпоинт схлопывания 720→860px (`smart-search.css:1874` до сих пор 720). Ветки: ветка А «на hh.ru» (активный сорсинг, конструктор 5 шагов, поллинг `GET /smart/runs/{id}`, ⚠️ тратит деньги клиента), ветка **В «Автоподбор»** (`.ssf-auto`, бейдж «+N новых» — сохранённые автопоиски резюме hh: список → кандидаты (пагинация, bottom-sheet превью на КЛАССАХ `cand-detail`, НЕ компонент) → основа оценки (вакансия/промт) → AI-оценка в фоне → «Забрать контакт» [⚠️ тратит контакты hh] → в пул/воронку; `frontend/src/pages/smart/SmartSearchAuto.tsx`, эндпоинты `/smart/auto/*`, эталон `project/components/SmartSearchAuto.jsx` + `11b`, память `autopodbor-feature-state`) ИЛИ ветка Б «по своей базе» (промт / по открытой вакансии → **мгновенный семантический косинус (pgvector)** + пилюли «N% совпадение» + кнопка **AI-оценки топ-N** [тратит токены, поллинг `GET /smart/base/runs/{id}`], история, «+ В вакансию» — см. §3). В обеих ветках сверху `‹ Выбор источника`. Эталон — `project/components/SmartSearch.jsx` (развилка+hh) + `project/components/SmartSearchBase.jsx` (ветка Б) + `project/styles/smart-search.css`. Хуки `useSmartSearch.ts`. Автофильтры vacancy-метода ветки Б — в стиле фильтров кандидатов (Должность/Город/Опыт/ЗП/Навыки, правки реально влияют на поиск). Индексация эмбеддингов — в **Настройки→AI**.
12. **Импорт кандидатов** (полноэкранный визард из шапки «Кандидаты», кнопка «Импорт кандидатов») — **развилка источника** (4 карточки 2×2): «Из файла» (Excel: загрузка → маппинг колонок → превью → импорт) и «Из Потока» (токен API → превью → импорт, без маппинга) — рабочие; «Talantix»/«Хантфлоу» — заглушки «Скоро» (`.imp-source-card.soon`, некликабельны, только вёрстка). Превью/результат/попап резюме общие. Эталон — `project/components/ImportCandidates.jsx` + `project/styles/import.css` + ТЗ `docs 12`. Бек — §3.
13. **Все вакансии** (`/vacancies/all`, v0.9.206-210) — сетка карточек всех вакансий + номерной пагинатор; пункт в сайдбаре со счётчиком (иконка `layout-grid`, стиль `.sub-archive`). `AllVacanciesPage.tsx` + `.css`. ⚠️ Грабля эры: `page_size 200` > серверный cap 100 → 422 и пустой экран (пофикшено `PER_PAGE`, v0.9.209).
14. **Публичные страницы (БЕЗ авторизации)** — не под Layout, company_id ТОЛЬКО из токена: (а) **запись на интервью** `/schedule/:token` (`pages/public/SchedulePage.tsx`, v0.9.221+ — слоты из календаря Б24, бронь, логотип, вью-пояс = браузер через `detectTz`; **с v1.1.0 — управление бронью: «Перенести»/«Отменить» с inline-подтверждением** (не `window.confirm` — §0), кнопки неактивны с объяснением при `can_change:false`; память `interview-scheduling-b24-state`); (б) **опрос Пульса** `/pulse/survey/#token` (память `pulse-public-survey-link`); (в) **форма заявки на подбор** `/apply/:token` (`pages/public/ApplyPage.tsx`, v0.9.240 — анонимная подача заявки, honeypot, company из ротируемого токена; память `hiring-requests-module-state`).
15. **Заявки на подбор** (`/requests`, `/requests/new`, v0.9.239-241) — список карточек + воронка-чипы этапов + панель заявки (оверлей, тулбар Перевести/Отклонить/Создать вакансию, Б24-полоса, тред уточнений, связанная вакансия с РЕАЛЬНЫМ прогрессом найма, история). Пункт «Заявки» закреплён СВЕРХУ подсписка «Вакансии» в сайдбаре (счётчик активных + бейдж +N). Роль **`hiring_manager`** видит ТОЛЬКО «Мои заявки» (read-only). Настройки воронки заявок — Настройки→«Воронка заявок». `RequestsPage.tsx`/`NewRequestPage.tsx` + `requests.css`. Память `hiring-requests-module-state`.

**⚠️ РОЛИ ТЕПЕРЬ 4 (`users.role` CHECK): admin, recruiter, manager, hiring_manager.** `hiring_manager` (нанимающий менеджер, v0.9.239) — класс изоляции ВНУТРИ компании: видит ТОЛЬКО свои заявки, больше НИЧЕГО. Изоляция DENY-BY-DEFAULT: гард `forbid_hiring_manager` (`core/permissions.py`) на include_router ВСЕХ data-роутеров в `router.py` — доступны лишь `/requests` (author-scoped) + `/auth/me`. Author-scoping обобщён на не-персонал (`_is_staff`=admin/recruiter; и `manager`, и `hiring_manager` в модуле заявок видят только свои). `UserCreate/Update.role`=Literal (422, не 500). Память `hiring-requests-module-state`, `rbac-state`.

> **Пагинатор — ТРИ КОПИИ, техдолг** (сверено грепом 2026-07-15, не верить «общему» на слово): `components/ui/Pager.tsx` (v0.9.219, единственный импорт — `pages/home/EventsFeed.tsx:6`, лента событий Главной, 10 на страницу); «Все вакансии» держит СВОЮ (`AllVacanciesPage.tsx:35 pageList` + разметка `.av-pager:138`); Автоподбор — ТРЕТЬЮ (`SmartSearchAuto.tsx:152 saPager`). Pager родился ПОЗЖЕ «Всех вакансий» (v0.9.219 > v0.9.210) и на неё не переводился. Новым экранам — брать `ui/Pager.tsx`; при следующей правке тех двух экранов перевести их на него (в отличие от Button/ScoreBadge/Avatar, которые исторически разошлись и скоупятся — память `shared-ui-components-diverge-from-etalon`).

---

## 5. КОНВЕЙЕР ПЕРЕСБОРКИ ФРОНТА ПО ЭТАЛОНУ (текущая фаза)

**Зачем:** первый фронт визуально разошёлся с дизайн-эталоном. Эталон — хэндофф из Claude Design в папке `project/` (HTML/CSS/JS-прототипы: `project/components/*.jsx`, `project/styles/*.css`, `project/Глафира Рекрутер (standalone).html`). Источник правды по визуалу: `project/styles/` + `frontend/src/styles/tokens.css` (ark-палитра + `--bg-*`/`--fg-*` через ark; шкала `--fs-11..32`; `--font-sans`=Inter, `--font-mono`=JetBrains Mono).

**Метод (Вариант 3):** пересобирать каждый экран 1:1 по эталону **КОНВЕЙЕРОМ — по одному экрану, с проверкой функционала после каждого.** НЕ всё разом (агент растеряет функционал/нагаллюцинирует).

**Жёсткие правила пересборки (на каждый экран):**
- Структура DOM + CSS — **буквально из эталона** `project/`.
- Функционал — **буквально из нашего текущего компонента** (хуки, мутации, состояния сохранить, ничего не выдумывать).
- НЕ переносить служебные артефакты прототипа: `data-screen-label`, фейковые данные, метки инструмента.
- CSS — только реальные токены (см. §0); эталонные hex смапить на токены, не вставлять как есть (иначе откат палитры).
- CSS **scoped** к корню экрана, НЕ глобально. **Исключение:** `.submenu-search` — глобальный примитив, его делит Архив; скоупить нельзя.
- Эталон местами хардкодит `font-size` в px — приводить КОНКРЕТНЫЕ значения к эталонным, не переводить массово на токены.
- НИКАКИХ `console.log` / mock / выдуманных данных / `alert()`.
- **Новый элемент, которого в эталоне нет** (эталон — create-only прототип без бэка; напр. счётчик кандидатов в редакторе этапов, инлайн-баннер серверной ошибки): точного 1:1 неоткуда — но СНАЧАЛА найти ближайший идиом в эталоне/проекте и повторить его, а не лепить своё. Счётчик этапа → идиом `.fc-count` (моно-число, без фона); ошибка формы → `.error-banner`.

**Иконки:** оставлены lucide-react (НЕ портируем из эталона — решение принято).

**Пересобраны и задеплоены:** Главная, Сайдбар (со списком вакансий), Layout, Воронка (каркас + карточка соискателя/7 табов + позиционирование detailMode), Перевести/Отклонить (карточка + bulk), форма создания вакансии (4 шага, кастомные этапы), форма добавления кандидата, редактирование воронки живой вакансии (CRUD этапов с защитой непустого/системного).

**Конвейер пересборки фронта по эталону — в основном ЗАВЕРШЁН** (последний релиз цикла `0.1.80`). С эры **0.2/0.3** идёт фаза интеграций и фич (hh/Avito/SMTP/Telegram/Bitrix/теги/пользователи/RBAC/Пульс — детали в памяти проекта) + точечные фичи поверх эталона. Правила пересборки выше остаются в силе для любого нового/трогаемого экрана.

> ⚠️ **В `project/` лежит НЕЗАКОММИЧЕННЫЙ ре-синк эталона от Claude Design — почти всё реализовано, но НЕ всё** (сверено грепом 2026-07-15; первая версия этой врезки утверждала «реализовано целиком» — адверс-проверка это опровергла, урок §0 в действии).
> - **Волна 1 (mtime 22.06) — ЗАКРЫТА:** `04a - Резюме - редизайн таба.md` + правки `Candidates.jsx`/`app.css` → v0.9.147 (`AIVerdictCard` ужат в одну строку `.filo-card-mini`, «Подробнее →» в правый край шапки, ScoreBadge xl→lg; опыт в раскладке hh: grid 134px/1fr, слева период+стаж, справа компания→сфера→должность→буллеты, маркер — чёрточка `::before content:'–'`, НЕ точка) + дофиксы v0.9.149/v0.9.213.
> - **Волна 2 (mtime 28.06) — ПОРТИРОВАНА ЧАСТИЧНО:** `11b - Автоподбор.md` + `SmartSearchAuto.jsx` + `smart-auto.css` были ВХОДНОЙ спецификацией — фронт НАЧАЛИ в тот же день (v0.9.156, 28.06) и довели до v0.9.197 (01.07). JSX/логика перенесены, **а CSS-диф `project/styles/smart-search.css` — НЕТ: 3-колоночная сетка развилки и брейкпоинт 860px не портированы** (см. §4 п.11 — открытый визуальный баг). Это реальный остаток бэклога.
> - **Сознательные отступления от 11b (НЕ баги, а соблюдение §0 «никаких фейк-заглушек»):** чипы фильтров автопоиска («с переездом», «за неделю» и т.п.) не рендерятся — бэк `AutoSearchItem` таких полей не отдаёт вовсе, пришлось бы выдумывать данные; чип подписки read-only `<span>`, а не «кликабельный тумблер» — среди 12 роутов `/smart/auto` нет переключения подписки, она живёт на hh. ⚠️ НО рядом сидит нарушение того же §0: кнопка `.ssa-sc-edit` («Изменить автопоиск на hh», `SmartSearchAuto.tsx:489`) — **БЕЗ `onClick` и `href`, выглядит рабочей (`cursor:pointer` + hover), но ничего не делает** — унаследована из create-only прототипа. Хвост: дать `href` на автопоиск hh или убрать.
> - ⚠️ **Токены:** в `smart-auto.css` эталона 4 фантома (`--bg-hover`, `--border-strong`, `--shadow-3` — БЕЗ фоллбэка, сломали бы вёрстку при копировании 1:1; `--ark-violet-600` — с фоллбэком, деградирует мягко). При реализации карта подмен §0 применена и задокументирована в шапке `SmartSearchAuto.css` — **в файлы Автоподбора ни один фантом не утёк** (полный скан чист). Это НЕ значит, что чист весь `frontend/src`: прежние фантомы там живы, см. список §0. **Копировать эталон 1:1 без карты подмен — сломать вёрстку.**

**Свежие фичи (0.3.x):** верификация частично реальная (DaData + OSINT claude-cli + «ПдН подписан», см. §2.8); рич-текст редактор описания вакансии (`RichTextField`, contentEditable+execCommand — `description` хранит HTML, в скоринг идёт со снятыми тегами); редактируемое «Описание этапа» воронки.

**Свежие фичи (0.4.x) — АВТОМАТИЗАЦИЯ ВОРОНКИ (⚠️ действует с живыми людьми, дефолт OFF, opt-in на вакансии):** см. память `funnel-automation-state`. П.1 автоперевод по скорингу + П.4 настраиваемый текст отказа — РЕАЛЬНЫ. П.2 (вопросы+автоперевод по ответам) и П.3 (автоотказ) — реализованы, тумблеры рабочие, дефолт OFF. **П.5 «Отправлять ссылку выбора времени интервью»** — см. эру 0.9.221+ ниже. **NB:** полный автоотказ+письмо (П.3) требует `glafira_mode='B'`, а селектора режима в UI НЕТ (`glafira_mode` зашит в 'A') → штатно сейчас П.3 = только подсказка `auto_reject_suggested_at`. Адверс-ревью v0.4.7 (131 агент) подтвердило предохранители (режим C/None не двигает, audit `actor_type='ai'`, only-forward, 502 цел, одна alembic-голова).
> **Хвосты автоматизации — статус пересверен грепом 2026-07-15.** ЖИВЫ (4): (а) автономный discard+письмо П.4 НЕ пишет ни `audit_log`, ни `Event` — **нарушение инварианта §2.2** (`services/integrations/hh/service.py:1467` `sync_company_rejections`, до конца файла :1639 — ни одного `audit(`, хотя в этом же файле audit есть на :219/:384/:430/:738/:799/:838/:918/:1249); (б) UI-подпись «при скоринге AI >» (`VacancyFormPage.tsx:1812`) vs код `score >= vacancy.auto_move_threshold` (`scoring.py:586`) — кандидат РОВНО на пороге поедет вперёд вопреки надписи; (в) `rejection_text` нельзя очистить в None: фронт шлёт `null` (`VacancyFormPage.tsx:1982`), бек молча игнорит (`services/vacancy.py:567` — `is not None` вместо `model_fields_set`, как в PATCH этапа) → паттерн «поле принимается схемой, но молча теряется» из §0; (е) П.3 бьёт по ВСЕМ нетерминальным этапам — единственный guard `if application.stage in ("rejected","hired")` (`auto_qa.py:285`), т.е. сработает и на `offer`, и на «Контакт с менеджером» (сравни: П.1 жёстко на `response` `scoring.py:583`, П.2 на настраиваемом `auto_qa_stage`). ПОЧИНЕНЫ МОЛЧА (2, из списка убраны): `glafira_mode` получил Pydantic-`Literal["A","B","C"]` (`schemas/vacancy.py:138,180` + CHECK в БД → 422, не 500); stale-коммента «П.2/П.3 disabled» в `VacancyFormPage.tsx` больше нет (актуальные комменты :417/:1831/:1942 говорят правду).

**Свежие фичи (0.5.x) — УМНЫЙ ПОДБОР / активный сорсинг (⚠️ ТРАТИТ РЕАЛЬНЫЕ ДЕНЬГИ КЛИЕНТА — квота hh + AI-токены + списание контактов):** новый раздел `/smart` (экран 11) — Глафира сама ищет резюме в базе hh, оценивает AI-скорингом против вакансии и приглашает лучших в воронку. Бек/предохранители — см. §3; UX — §4 п.11; глубокие детали и грабли — память `smart-search-state` ([[hh-resume-search-recon]]). Модель доступа: поиск+оценка без платного доступа, приглашения только при платном доступе + опубликованной на hh вакансии (иначе превью-режим). Фильтры (проф-область/роль hh) подбирает Глафира из вакансии (LLM). **Первый реальный платный запуск делает заказчик осознанно** — не агент. Открытый риск: сам `GET /resumes/{id}` для оценки МОЖЕТ требовать платный доступ hh (проверяется на живом запуске по диаг-логу).

**Свежие фичи (0.6.x) — ИМПОРТ КАНДИДАТОВ:** новый экран 12 (визард с развилкой источника). 0.6.0–0.6.7 — импорт из Excel (.xlsx); 0.6.8 — вторая ветка «Из Потока» (potok.io) по офиц. API-токену (см. §3, память `potok-resume-import`); 0.6.9 — развилка из 4 карточек (+ «Скоро»-заглушки Talantix/Хантфлоу), кнопка входа «Импорт из файла»→«Импорт кандидатов»; 0.6.10 — кнопки «Назад/Далее» визарда как в форме вакансии/кандидата (`btn-sm`+chevL+arrowRight). Память `candidate-import-excel-state`. Параллельно (инфра): `provision_company.py` (CLI безопасного провижининга 2-го арендатора — в системе РЕАЛЬНЫЙ 2-й клиент, см. `multitenancy-isolation-audit`), `scripts/backup_all.sh` (полный бэкап БД+.env+том).

**Свежие фичи (0.7.x) — УМНЫЙ ПОДБОР ПО СВОЕЙ БАЗЕ (заход A, SQL):** 0.7.0 — развилка источника в `/smart` + ветка Б «по своей базе» (SQL-поиск + LLM-парс промта); 0.7.1 — автофильтры vacancy-метода в стиле фильтров кандидатов. Память `smart-search-state`.

**Свежие фичи (0.8.x–0.9.2) — СЕМАНТИЧЕСКИЙ ПОДБОР ПО СВОЕЙ БАЗЕ (заход B, ГОТОВ):** pgvector-эмбеддинги резюме (локальная ONNX fastembed, `candidate_embeddings`), мгновенный косинус-retrieve по всей базе + AI-rerank топ-N по кнопке (гибрид). 0.8.0 семантический слой; 0.8.1–0.8.2 индексация во вкладке Настройки→AI; 0.8.3 async-поиск (фикс 504); 0.8.4 грабля миграции (`server_default` через `sa.text()`); 0.8.5 + 0.9.0–0.9.2 anti-hang + гибрид (мгновенный косинус + один инпут N для AI-оценки). Детали/грабли — см. §3 + память `smart-search-state`.

**Свежие фичи (0.9.3–0.9.12) — ПОВЕРХ ЭТАЛОНА:** 0.9.3 хаб источников импорта (15 карточек); 0.9.4 скачивание резюме в пуле; 0.9.5 экспорт PDF/DOCX в стиле hh (reportlab+python-docx, память `resume-export-feature`); 0.9.6 вкладка Настройки→AI + **per-company выбор LLM-модели** (память `ai-model-per-company`); 0.9.7–0.9.8 добавление кандидата с автозаполнением из резюме (память `add-candidate-resume-autofill`); 0.9.9–0.9.10 удаление кандидата (память `candidate-delete-and-button-scoping`); 0.9.11 **зарплата-вилка** salary_from/salary_to (память `salary-range-sync-invariant`); 0.9.12 дедуп при ручном добавлении (память `candidate-dedup-manual-add`).

**Свежие фичи (0.9.13–0.9.29) — АУДИТ + ТЕСТ-ДОЛГ (стабилизация):** 0.9.13–0.9.16 фиксы HIGH глубокого бэкенд-аудита; 0.9.21–0.9.22 Medium-бэклог (индексы, надёжность импорта, evaluate-TOCTOU); 0.9.24–0.9.29 разгребание тест-долга полного прогона → **713/713 зелёных** (попутно 9 реальных прод-багов). Память `backend-deep-audit-0-9`, `full-suite-test-debt`.

**Свежие фичи (0.9.30) — ШАБЛОНЫ СООБЩЕНИЙ:** новый раздел Настройки→«Шаблоны сообщений» (CRUD: название + плоский текст, общие на компанию, БЕЗ плейсхолдеров) + выпадашка «Шаблон» в чате карточки кандидата (быстрая вставка текста в поле ввода). Отдельный роутер `/message-templates` (read — все роли, write — admin+recruiter; НЕ под `settings_permission_dependency`). Память `message-templates-state`.

**Свежие фичи (0.9.31–0.9.32) — ГЛАВНАЯ:** баланс колонок шаблонов; блок «Последние сообщения»/чаты (`GET /home/dialogs`) + контекст кандидат/вакансия в ленте событий (см. память `home-features-and-etalon-sync`).

**Свежие фичи (0.9.33–0.9.34) — ЗВОНКИ MANGO OFFICE:** интеграция телефонии в карточку кандидата (новый таб «Звонки»). 0.9.33 — настройка интеграции (ключи Fernet в generic `integrations`, `provider='mango'`) + клиент Mango API. 0.9.34 — выгрузка истории звонков поллингом (двухшаговый `/vpbx/stats/*`), матчинг по нормализованному номеру (`find_duplicate_candidates`, СТРОГО company-scoped), прослушивание записи (проксирование 302→MP3), **расшифровка ПО ЗАПРОСУ + кэш через Gemini-via-OpenRouter** (`GLAFIRA_TRANSCRIBE_MODEL='google/gemini-2.5-pro'`, существующий `OPENROUTER_API_KEY`; образец — соседний проект ArkadyJarvis) + AI-разбор через `call_json`. Записи/расшифровки БЕЗ гейта согласия (решение заказчика). ⚠️ Точный парсинг CSV Mango (направление/время/external_id) и услуги тарифа — **требуют пиннинга на реальном ключе заказчика**. Память `mango-calls-integration-state`.

**Свежие фичи (0.9.41–0.9.46) — СУПЕРАДМИНКА ТЕНАНТОВ (отдельный сервис, развёрнут на VPS):** `backend/superadmin/` — изолированный FastAPI (Jinja UI, своя env-авторизация `SUPERADMIN_*`, profiles:[superadmin], за nginx `/super/`). CRUD компаний через `provision_company` + per-company OpenRouter-ключ (Fernet, маскирован) + просмотр junit-результатов. НЕ импортируется в клиентское приложение. 0.9.42 фикс рендера (Starlette `TemplateResponse` request-first), 0.9.43 хост-порт через `SUPERADMIN_PORT` (.env; на VPS 8001 занят → **18001**), 0.9.44 относительный action формы (по http POST уходил мимо/без Secure-cookie), 0.9.45–0.9.46 версия Глафиры в шапке (маунт ПАПКИ `frontend/src/lib` → читает version.ts на рендер). ⚠️ Авто-пересборка суперадминки в `deploy.yml` есть, но `.github/workflows/*` нельзя пушить токеном без scope `workflow` → правки deploy.yml вносятся через GitHub web UI. Память `superadmin-service-state`.

**Свежие фичи (0.9.48) — ЛЁГКИЙ БИЛЛИНГ:** `companies.paid_until` (Date nullable) + жёсткий дизейбл тенанта 402 `SUBSCRIPTION_EXPIRED` при просрочке/NULL — см. инвариант §2.9. Гейт в `get_current_user`; дата правится в суперадминке (поле «Оплачено до» + красный флаг просрочки в списке); фронт показывает полноэкранный `SubscriptionExpiredScreen`. Тесты: conftest autouse `_bypass_billing_gate` (по умолчанию off) + маркер `billing_gate` для тестов гейта. Полный pytest на VPS зелёный (2026-06-14). ⚠️ NULL=заблокирована → существующим компаниям задать `paid_until` в суперадминке сразу. Память `billing-paid-until-state`.

**Свежие фичи (0.9.62–0.9.65) — TELEGRAM: ДВУСТОРОННЯЯ ПЕРЕПИСКА С КАНДИДАТОМ:** канал Telegram доведён из «теста себе» до реальной переписки в карточке кандидата. Детали/грабли — память `telegram-integration-state`.
- **0.9.62 — вход по QR-коду** (`ExportLoginToken`/`ImportLoginToken`, stateless/DB-backed под 2 воркера, образец ArkadyJarvis `gen_session_qr.py`): SMS-код не доходил → теперь подключение аккаунта сканированием QR (Настройки→Интеграции→Telegram). 2FA переиспользует `confirm_password`. `TELETHON_API_ID/HASH` в env (приоритет над `TELEGRAM_*`).
- **0.9.63 — реальная отправка кандидату:** раньше `message.py::send_message` для `channel='telegram'` молча писал в карточку без отправки (фейк). Теперь `_send_telegram` шлёт через user-аккаунт компании (`send_to_peer`: резолв пира username→телефон через `_resolve_peer`/ImportContacts, телефон нормализуется). Каналы чата: **whatsapp/max убраны**, **sms = «Скоро»** (disabled — SMS-провайдера нет, не делаем вид), активны telegram/hh/email.
- **0.9.64 — обратная синхронизация (ответы кандидатов в чате):** зеркалит hh-поллер. `tg_user_id` пишется в `candidate.extra` при отправке; `sync_inbound` сохраняет входящие как `Message(direction='in', sender_type='candidate', external_id='tg:{peer}:{msg}')`, дедуп, строго company-scoped, только диалоги сопоставленных кандидатов (личные чаты рекрутёра не читаются). Триггеры: `POST /candidates/{id}/messages/telegram/sync` при открытии чата + поллинг 12с + резинк 90с (in-flight guard) + cron `app/jobs/poll_telegram_messages.py` (flock, раз в 3 мин, **заказчик добавляет в crontab вручную**).
- **0.9.65 — фикс «открыл чат — пусто»:** синк одного кандидата (открытие чата) теперь резолвит диалог НАПРЯМУЮ (`fetch_candidate_inbound`, как при отправке), не завися от заранее сохранённого `tg_user_id` и окна топ-N диалогов; бэкфиллит `tg_user_id`. Cron-путь (company-wide) — по-прежнему `iter_dialogs`.
> ⚠️ Между 0.9.48 и 0.9.62 были и другие деплои (hh-фиксы, Поток и т.д.) — здесь зафиксирована только Telegram-эра; при сомнении сверять `git log`.

**Свежие фичи (0.9.66–0.9.77) — ТЕЛЕФОНЫ + ПАРСИНГ ФАЙЛА ВАКАНСИИ:** единый формат телефонов (хранение цифрами без `'+'`, маска `PhoneInput` на react-international-phone, нормализатор для ВСЕХ писателей — память `phone-format-e164`, v0.9.66-69); скачивание PDF резюме hh в Документы кандидата (0.9.72); AI-парсинг файла вакансии PDF/DOCX с предзаполнением формы (`POST /vacancies/parse-file`, переиспускает резюме-механизм — память `vacancy-file-parse-prefill`, v0.9.75-77).

**Свежие фичи (0.9.79–0.9.108) — УГЛУБЛЕНИЕ УМНОГО ПОДБОРА hh + АВТО-РУБРИКАТОР СКОРИНГА:** «Забрать к себе» (открыть контакт hh без negotiation, `source='smart'`, +bulk, v0.9.79-82); авто-генерация рубрикатора критериев оценки из вакансии + кнопка «Сгенерировать критерии» (v0.9.83-89); справочник ролей hh `GET /smart/role-categories` + профобласть→роль browse + **multi-block hh search (роль и навыки отдельными text-блоками)** + skill-filter exact/soft — ⚠️ **отсюда форма `debug_params = {structural, text_blocks, skill_filter}`** (v0.9.90-100); показ непрошедших порог отдельной секцией + экспорт резюме hh с AI-разбором (v0.9.103-108). Память `smart-search-state`, `ai-model-per-company`.

**Свежие фичи (0.9.109–0.9.126) — ХАБР КАРЬЕРА + АВИТО РАБОТА (новые источники откликов):** рефактор источника — добавлен `habr` в `Candidate.source`, удалён мёртвый `Application.source` (память `candidate-source-model`, v0.9.109-111); **Хабр Карьера** — OAuth + синхронизация откликов→воронка + развязка контактов по кнопке (таблицы `habr_integrations`/`habr_oauth_states`, память `habr-oauth-scaffold`, v0.9.112-122); **Авито Работа** — синхронизация откликов→воронка (телефон в отклике бесплатно, таблица `avito_integrations`, память `avito-job-responses-sync`, v0.9.123-126).

**Свежие фичи (0.9.127–0.9.143) — 4 ВОЛНЫ SECURITY-АУДИТА + ФИКСЫ (0 Critical):** четыре сплошных аудита бэка через Workflow (адверс-верификация каждой находки) + фикс-форвард. 6 LOW (verify-идемпотентность, OSINT-consent guard и др., v0.9.127-128); **аудит-2** TOP-5 (RBAC-менеджер только назначенные вакансии / unique-индексы Application+AiEvaluation / Habr-TOCTOU / account-lockout / PII из промпта, v0.9.129-132 — память `security-audit-2-top5-fixes`); **аудит-3** 1 HIGH path-traversal + 8 MEDIUM (mask_config whitelist, RBAC calls/home, filename-санитизация/zip-bomb-кап, enum→Literal=422, take whitelist, v0.9.133-138 — память `security-audit-3-findings`); **аудит-4** 1 HIGH MONEY-1 (кап `ai_score_attempts` авто-скоринга) + 7 MEDIUM (analytics/clients/pulse RBAC, Mango/Bitrix SSRF-гард, call_sync TOCTOU partial-unique, hh refresh-гонка FOR UPDATE, v0.9.139-143 — память `security-audit-4-findings`). Все 4 аудита: 0 Critical. Память `rbac-state`.

**Свежие фичи (0.9.144) — ЧИСТКА ТЕСТ-ДОЛГА (раунд 2):** полный pytest на VPS вырос до **1103 тестов**; после security-эры всплыло 24 падения — триаж 12-агентным Workflow (следователь+адверс-верификатор на кластер, git-blame) → **ВСЕ 24 протухшие тесты, 0 реальных багов, инварианты целы**. Починены в тестах (прод-код НЕ тронут) → **1103/1103 зелёных**. Память `full-suite-test-debt`.

**Свежие фичи (0.9.145–0.9.146) — ОТОБРАЖЕНИЕ ОПЫТА РАБОТЫ:** вкладка «Резюме» карточки кандидата — сортировка позиций «свежие сверху» + длительность каждой позиции под датами + общий стаж по месяцам (фронт-парсер `period` в `ResumeTab.tsx`, 0.9.145); бэк-парсер периодов `candidate.py` научился читать `YYYY-MM` (точные месяцы, не теряет внутри `2005-04`) — чинит `total_experience`/`last_job_tenure`/`pick_latest_experience`/recency-ключ + порядок опыта в экспорте резюме по свежести (`resume_export.py`, 0.9.146). `period` хранится СТРОКОЙ (формат hh `_hh_period`: `«YYYY-MM — YYYY-MM»` / `«… — по наст. время»`; LLM-формат `«2020-2022»`/`«Июл 2018 — Авг 2019»` тоже поддержан). Тесты — `test_latest_experience.py`.

**Свежие фичи (0.9.147–0.9.203) — сессия точечных фич/фиксов:** точечная оценка ОДНОГО кандидата в Автоподборе (кнопка «Оценить» в AI-табе карточки, активна при заданной основе; single-прогон `note='[single]'` исключён из прогресса, но входит в мёрж баллов, v0.9.197); дата найма сотрудника Пульса — правка постфактум в карточке (`PATCH /pulse/employees/{id}/hire-date`, v0.9.198) + запрос **«Даты выхода»** при переводе в «Нанят» в воронке (`MoveRequest.hire_date`→`start_date`; «День X»/дедлайны плана относительны, обратно совместимо, v0.9.199); фикс создания пользователя **405** (эндпоинт на `@router.post("")` а не `"/"` — `redirect_slashes=False`, v0.9.200); меню действий у последнего юзера открывается ВВЕРХ при нехватке места (v0.9.201); фикс `scripts/backup_db.sh` (не сорсит `.env` — падал на bcrypt-хэше `$2b$` под `set -u`; перевёл на `read_env` grep+cut как в `backup_all.sh`, v0.9.202); **«Текст отказа по умолчанию» перенесён** из «Общие→Настройки Глафиры» в «Шаблоны сообщений», плейсхолдер = встроенный текст отказа воронки (вынесен в `frontend/src/lib/rejection.ts`; RBAC: правит только admin — `PATCH /settings/glafira` admin-only, рекрутёр видит read-only, v0.9.203). ⚠️ Автоподбор-эра (0.9.181-0.9.196: self-heal arq-очередь, параллельный скоринг ×8, `hh_sync.log`/`auto_eval.log`, глобальная сортировка/URL/пагинация) — см. память `autopodbor-feature-state`/`background-jobs-arq-queue`/`smart-search-state`.

**Свежие фичи (0.9.204) — ИМПОРТ ВАКАНСИЙ С hh + удаление фото-бэкфилла:** новая секция в карточке hh (Настройки→Интеграции, admin, только когда подключён): **«Показать вакансии с hh»** → список вакансий работодателя (`GET /integrations/hh/vacancies` теперь с `linked:bool`) → **«Создать все новые (N)»** → создаёт вакансии у нас и **привязывает** (`hh_vacancy_id`, `external_source='hh'`) — отклики придут сами кроном `poll_hh_responses` (**соискателей НЕ импортируем**). Смысл: подключил организацию с готовыми вакансиями на hh — забрал одним махом, не пересоздавая/не привязывая руками. Бек: `client.get_vacancy_by_id` (`GET /vacancies/{id}`, best-effort None; **БЕСПЛАТНО** — не просмотр резюме, квоту не тратит); `service.import_hh_vacancies` (переиспускает `create_vacancy`→этапы/причины/audit; маппинг name/city=area.name/description(HTML)/salary/employment_type=employment.id/`team=[actor]`; **RUR→RUB**; skip уже привязанных; per-vacancy `begin_nested`+commit+rollback как `candidate_import`; кап 40 стр.); `POST /integrations/hh/vacancies/import` (require_admin, тело `{hh_vacancy_ids?: list[str]|None}`→все новые). **УДАЛЁН** ручной фото-бэкфилл (`services/photo_backfill.py`, `POST /candidates/photos/backfill`, `useCandidatePhotos.ts`, UI-подсекция «Фото кандидатов с hh») — не актуален; фото-ПРОКСИ новых кандидатов (`photo_proxy.py`, avatar_url) НЕ тронут. Делали субагенты (fastapi/react-expert, model override — их дефолт-модель недоступна), верифицировано постфактум (git diff/grep/tsc/build/py_compile + адверс-ревью 3 агента → GO, 0 блокеров, 2 LOW пофикшены). Память `hh-integration-state`.

**Свежие фичи (0.9.206–0.9.210) — ЭКРАН «ВСЕ ВАКАНСИИ»:** отдельный роут `/vacancies/all` (сетка карточек + СВОЙ локальный пагинатор `.av-pager` + пункт сайдбара со счётчиком) — см. §4 п.13. Позже, в v0.9.219, из ленты событий Главной вырос общий `components/ui/Pager.tsx` — но «Все вакансии» на него НЕ переведены (три копии пагинатора, см. врезку в §4).

**Свежие фичи (0.9.211–0.9.220) — ТОЧЕЧНЫЕ ФИКСЫ:** поиск по ФИО словами в ЛЮБОМ порядке в ДВУХ местах одинаково (воронка `services/application.py:73-95` + пул `services/candidate.py:303-325`; каждое слово OR по last/first/middle_name, слова между собой AND — «Роман Г» находит «Грузо Роман»; `middle_name` в поиске раньше не было вовсе, v0.9.211); комментарий из формы добавления кандидата больше НЕ теряется в `extra['comment']` (где не отображался) — создаётся реальная запись `Comment` в ленту (v0.9.212); HTML из описания опыта импорта Потока чистится в плоский текст (`potok/mapper.py::_html_to_text` + `lib/htmlToText.ts` — теги текли в карточку/экспорт/скоринг, v0.9.213); **Telegram** (v0.9.214-218, память `telegram-integration-state`): `resolvePhone` ПЕРВЫМ (резолв номера БЕЗ добавления в контакты), кэш `username` кандидата в `extra` после резолва (следующие сообщения без контактов), контакт добавляется с реальным ФИО (а не «Кандидат»), честная ошибка `TG_IMPORT_LIMITED` вместо тихого сбоя, fast-path по `tg_user_id`, журнал `TG_SEND_LOG_PATH`; чат показывает РЕАЛЬНУЮ дату сообщений вместо «сегодня» (v0.9.216); ошибка неверного пароля на логине стабильна и не двигает кнопку (v0.9.220).

**Свежие фичи (0.9.221–0.9.235) — ЗАПИСЬ НА ИНТЕРВЬЮ ЧЕРЕЗ КАЛЕНДАРЬ БИТРИКС24 (главная фича эры, 15 коммитов):** кандидат получает персональную ссылку → публичная страница со свободными слотами → бронь создаёт событие в календаре Б24 + видеокомнату + шлёт ICS-приглашение. Глубокие детали и ГРАБЛИ формата Б24 — память `interview-scheduling-b24-state` (⚠️ формат реверс-инжинирен по РАБОЧЕМУ эталону ArkadyJarvis — верить ему, не догадкам). Каркас:
- **Модель `InterviewLink`** (`models/interview_link.py`, таблица `interview_links`: token `secrets.token_urlsafe(32)`/status `active|booked|expired`/expires_at (14 дней)/slot_from/slot_to/b24_event_id). Миграция `f5a6b7c8d9e1` = **текущая голова** (+ `users.b24_user_id`, + `vacancies.auto_interview`/`auto_interview_stage`, + CHECK `Event.type` со значением `'interview'`).
- **ТРИ ПУБЛИЧНЫХ РОУТА БЕЗ АВТОРИЗАЦИИ** (`api/v1/public_schedule.py`, prefix `/public` в `router.py`): `GET /public/schedule/{token}`, `GET …/slots`, `POST …/book`. company_id — ТОЛЬКО из токена. ⚠️ Фронт `publicClient` обязан бить в `/public/…` — забыть префикс = 404. Защита: in-memory rate-limit 30/60с на пару IP:token (`_rate_store` — НЕ переживает рестарт, НЕ общий на 2 воркера). Наружу отдаётся vacancy_name + ФИО/аватары участников; телефонов/email/ID кандидата НЕТ. Бронь атомарна: conditional UPDATE `active→booked` ПЕРЕД созданием события в Б24, при сбое Б24 — откат в `active` + 503.
- **Слоты** (`integrations/bitrix24/interview_slots.py::calculate_free_slots`): свободно = окно минус ОБЪЕДИНЁННАЯ занятость всех участников (модель ArkadyJarvis). Часы ЗАХАРДКОЖЕНЫ решением заказчика (v0.9.223 «убраны настройки слотов»): Пн–Пт 09:00–19:00, слот ТОЛЬКО полный часовой (хвост <60 мин отбрасываем, v0.9.232), лид 24ч, горизонт 14 дней. Пояс — АВТО из Б24 (`resolve_interview_tz`: TIME_ZONE рекрутёра → портал → Москва). ⚠️ Эндпоинты `GET/PATCH /integrations/bitrix24/schedule-settings` и хуки `useB24ScheduleSettings`/`usePatchB24Schedule` ОСТАЛИСЬ, но ни один `.tsx` их не использует — **мёртвая поверхность**. Из config интеграции реально читаются лишь: `interview_video_link` (ручная ссылка, приоритет над авто-генерацией — `public_schedule.py:559 if not video_link:`); `horizon_days` — ТОЛЬКО в `interview_schedule.py:46,81` (срок жизни ссылки + текст письма), в расчёте слотов НЕ участвует (там жёстко `_FIXED_SLOTS`, 14 дней); `tz` — только в info-роуте (`public_schedule.py:360-361` → `ScheduleInfoResponse.tz`). ⚠️ Нюанс: авто-пояс из Б24 (`resolve_interview_tz`) работает в `/slots` и `/book`, а info-роут отдаёт config-пояс с фолбэком на Москву — фронт его игнорит (показывает по `detectTz` браузера).
- **Scope вебхука Б24 теперь `user` + `calendar` + `im`** (раньше хватало `user`): `calendar.accessibility.get` (занятость, fail-closed — при сбое `AppError`, НЕ пустой dict), `calendar.event.add` (встреча), `im.chat.add`+`im.dialog.get` (**авто-генерация УНИКАЛЬНОЙ видеокомнаты Б24 на каждое интервью** = чат `ENTITY_TYPE=VIDEOCONF` → `result.public.link`; graceful→None, бронь не валится; ⚠️ плодит VIDEOCONF-чаты без чистки). Внешнего гостя в событие Б24 через REST добавить НЕЛЬЗЯ → кандидату шлём **ICS** (`send_email(ics=…)` → `multipart/mixed[alt(text,html), text/calendar]`, METHOD:REQUEST) + дубль ссылки в hh-чат best-effort после коммита.
- **П.5 автоматизации воронки** — `vacancy.auto_interview` + `auto_interview_stage` (дефолт OFF, opt-in, тумблер в форме вакансии), рассылка `services/glafira/interview_schedule.py::send_interview_links` (идемпотентно по активной ссылке; незамапленным участникам — Event ОДИН раз, v0.9.225). Требует `b24_user_id` у команды вакансии → кнопка **«Сопоставить всех по email»** (`POST /integrations/bitrix24/users/map-all`, admin) + импорт из Б24 проставляет id сразу (v0.9.226). ⚠️ **НОВОГО КРОНА НЕТ** — рассылка встроена в существующий `app/jobs/score_pending.py` (+24 строки): без него в crontab на VPS П.5 просто не работает.
- Попутно: `core/errors.py::http_exception_handler` теперь ПРОБРАСЫВАЕТ уже готовый конверт `detail={"error":{…}}` без двойной обёртки (раньше `str(detail)` заворачивал dict второй раз) — публичные роуты отдают читаемые 409 SLOT_TAKEN / 410 / 503. Автоматизации (auto_qa) стали видны в ленте «Все действия» (Event `type='qual'`, `actor_type='ai'`, v0.9.224).

**Свежие фичи (0.9.236–0.9.238) — ГЛАФИРА НАЗЫВАЕТ КОМПАНИЮ-ЗАКАЗЧИКА КАНДИДАТУ:** во всех исходящих текстах (чат, письма, приглашение/страница интервью, встроенный отказ, hh-приглашение) Глафира называет компанию вакансии, а не только «Глафира Рекрутёр». Хелпер `services/company_display.py::resolve_company_display_name` (заказчик `vacancy.client` → фолбэк на компанию-арендатора; ⚠️ `Vacancy.client` lazy → читать `client_id`, не `.client`, иначе MissingGreenlet). Заказчик ОБЯЗАТЕЛЕН в форме вакансии (валидация ТОЛЬКО в роуте POST/PATCH, НЕ в схеме/БД — иначе ломается hh-импорт и миграция), инлайн-создание заказчика в форме для рекрутёра. Память `company-name-in-outbound-texts`.

**Свежие фичи (0.9.239–0.9.241) — МОДУЛЬ «ЗАЯВКИ НА ПОДБОР» (крупная фича, экран 15):** нанимающий менеджер подаёт заявку (кабинет ИЛИ анонимная публичная форма `/apply/:token`) → рекрутер ведёт по воронке заявок → из заявки создаётся вакансия (1:1, `VacancyCreate.request_id`) → прогресс найма РЕАЛЬНЫЙ из воронки вакансии → автозакрытие при найме всех позиций (единой точкой в `move_application`). **Новая роль `hiring_manager` — deny-by-default изоляция** (см. §4 п.15). Публичная форма — паттерн `/schedule` (токен/ротация/rate-limit/honeypot, company из токена). Уведомления честные (email юзеру, внешним авторам формы — нет, текст «рекрутер свяжется по контакту»). Ревью 5 скептиков: 0 blocker/high, изоляция airtight; фикс-форвард (изоляция старого `manager` в заявках, dead-кнопки, edge автозакрытия, escape untrusted HTML). ⚠️ Грабли: коллизия revision id миграции (проверять грепом), циклическая FK vacancies↔hiring_requests (post-create FK), autoflush=False → явный flush перед подсчётом hired. Память `hiring-requests-module-state`.

**Свежие фичи (1.0.3–1.0.7) — ФИКСЫ КЭША СЧЁТЧИКОВ (жалобы «цифры скачут/старые»):** счётчики этапов НЕ должны считаться из клиентского/отфильтрованного/постраничного списка — только серверный агрегат. Память `query-cache-staleness-funnel`.
- **v1.0.4** — инвалидация мутаций расширена: `applications.ts`/`candidates.ts`/`vacancies.ts` в onSuccess инвалидируют `['home']`, `['vacancies','sidebar']`, `['requests']` (+ `['vacancies',id,'applications'|'stages']`). Грабля: ключ сайдбара — `['vacancies','sidebar']`, НЕ `['sidebar']` (был баг).
- **v1.0.6 — «цифры этапов ВОРОНКИ ВАКАНСИИ скачут»:** НЕ кэш. `reorder` этапов даёт `order_index=1..N`, а `addStage` (`VacancyFormPage.tsx`) слал `order_index=stages.length-2` → коллизия с существующим этапом → `get_vacancy_stages` сортировал ТОЛЬКО по `order_index` без тай-брейкера → Postgres при равных индексах отдаёт ряды в произвольном порядке → чипы переставляются между рефетчами. Фикс: `.order_by(order_index, stage_key)` во всех 3 чтениях этапов в `services/vacancy.py` (stage_key уникален на вакансию). (v1.0.5 `staleTime:0` — откачен, лечил не то.)
- **v1.0.7 — «цифры этапов ЗАЯВОК скачут» (клик по этапу → все 0):** `RequestsPage` считал counts чипов из `useRequests({status})` — отфильтрованного списка. Фикс (как в воронке): `GET /requests/stages` (`get_stage_flow(session, company_id, user)`) отдаёт `count` на этап — grouped-агрегат по ВСЕМ заявкам с изоляцией списка (не-персонал → author-scoped); фронт читает `stage.count`.

**Свежие фичи (1.0.8–1.0.10) — ТЕСТ-ДОЛГ РАУНД 3 + 3 РЕАЛЬНЫХ БАГА** (полный pytest вскрыл 81 failed + 1 error после эр «обязательный заказчик» v0.9.238 + модуль заявок; фикс тестов — 18-агентным Workflow, диффы сверены руками; память `full-suite-test-debt`):
- **РЕАЛЬНЫЙ БАГ — публичный OAuth-callback hh/habr под auth-гардом:** модуль заявок повесил `_deny_hm` (`forbid_hiring_manager`→`get_current_user`) на весь `integrations_router` → публичный редирект провайдера (браузер без нашей куки) получал 401 вместо 307. Фикс: callback'и вынесены на отдельный `public_router` (в `integrations.py`) без `_deny_hm`, монтируется в `router.py` отдельно (безопасность — через `state`). **Урок: router-level auth-гард требует авторизацию на ВСЁМ роутере — публичные эндпоинты (OAuth-callback) держать на отдельном роутере БЕЗ гарда.**
- **РЕАЛЬНЫЙ БАГ — коммент заявки не возвращался:** `add_comment` создавал `RequestComment(request_id=req.id)` без синка ORM-связи → при `expire_on_commit=False` повторный `get_request_or_raise` в том же запросе отдавал тот же инстанс со stale-пустой `comments` → POST коммента возвращал деталь БЕЗ нового коммента. Фикс: `request=req` (back_populates наполняет `req.comments`). См. граблю identity-map в §3.
- **ЦИКЛИЧЕСКИЙ FK vacancies↔hiring_requests ронял `metadata.drop_all` в тестах** (`CircularDependencyError`). ⚠️ 1-я попытка (`use_alter`+`name`, v1.0.8) СЛОМАЛА ВСЁ (явное имя → `DROP CONSTRAINT` по имени, которого в персистентной тест-БД нет → все тесты ERROR). Правильно (v1.0.9): use_alter УБРАН (create_all цикл разруливает сам), сброс схемы в `conftest.py::test_engine` — `DROP TABLE IF EXISTS <все metadata.tables.values()> CASCADE` (без сортировки → не падает на цикле; CASCADE снимает FK; не требует прав владельца схемы). **Урок: НЕ ставить `use_alter`+name на FK при персистентной тест-БД (имя не совпадёт); циклы чистить `DROP TABLE CASCADE`, не `metadata.drop_all`.**
- **ПРОТУХШИЕ ТЕСТЫ (~74):** обязательный заказчик — conftest-фикстура `default_client` (создаёт Client, отдаёт id) + `client_id` в ~50 тестах POST /vacancies (15 файлов); интервью-слоты (заморозка `now()` — тест зависел от реального дня недели при `horizon_days=1`; ключи мока Б24 `FROM/TO`→`DATE_FROM/DATE_TO`; rate-limit ключ по `X-Forwarded-For`); коммент кандидата (`Comment` вместо `extra`, v0.9.212); company_display (NameError в class-body); hh_polling (`stats`→`result`); `test_delete_vacancy_keeps_request` (удалить отклики перед hard-delete + `refresh()` от stale identity-map).

**Свежие фичи (1.0.11) — ПАГИНАЦИЯ ВОРОНКИ (жалоба «список кончается на 25 кандидатах»):** воронка вакансии рендерила ТОЛЬКО первую страницу (`page_size` бека = 24) и пагинации не имела вовсе — `total`/`pages` из ответа игнорировались. При сортировке по колонке «AI» это означало ровно 24 лучших по баллу, остальные недостижимы (симптом заказчика: «видны только высокобалльные, скролл зависает»). CSS-блокировки скролла НЕ было — список физически кончался. Починка: `useApplicationsInfinite` на `useInfiniteQuery` по идиому пула (`useCandidates`), автодогрузка через `IntersectionObserver` с **`root` = элемент `.cand-scroll`** (⚠️ скроллится контейнер, а НЕ окно — с `root: null` догрузка не сработает), `rootMargin: 200px`. Ключи `['vacancies',id,'applications','list'|'one',filters]` — сегмент ПОСЛЕ `'applications'`, чтобы префиксная инвалидация мутаций не сломалась. **Попутно скрытый баг: фронт слал `size`, бек читает `page_size`** (`applications.py:62`) → размер страницы молча терялся (паттерн §0 «поле принимается схемой, но молча теряется»). `VacancyDetailPage` переведён на тот же запрос (общий кэш, без второго HTTP).

**Свежие фичи (1.1.0–1.1.3) — ОТМЕНА И ПЕРЕНОС ВСТРЕЧИ КАНДИДАТОМ (надстройка над записью на интервью):** раньше после брони кандидат оказывался в тупике — ссылка жила, страница показывала «Встреча назначена», но откатить/перенести было нельзя. Теперь управление живёт на ТОЙ ЖЕ ссылке из первого письма (новая рассылка не нужна).
- **Решения заказчика:** отмена ОСВОБОЖДАЕТ слот (ссылка → `active`, кандидат выбирает заново; новый статус НЕ заводили, CHECK не трогали); дедлайн изменений — **24 часа** до начала; **максимум 2 переноса**; уведомлять ВСЕМИ каналами.
- **Бек:** миграция `b3c4d5e6f7a8` (+`interview_links.reschedule_count`); `POST /public/schedule/{token}/cancel` (без тела, публичный, rate-limited) → 409 `NOT_BOOKED`/`RESCHEDULE_LIMIT`/`CHANGE_DEADLINE_PASSED`; `ScheduleInfoResponse` +`reschedule_count`/`reschedules_left`/`can_change`/`change_blocked_reason` (**правила считает сервер — фронту не дублировать**). Освобождение — атомарный conditional UPDATE `booked→active` с `RETURNING` (двойной клик → 409) + ручная синхронизация ORM-инстанса после сырого UPDATE (грабля identity-map). `expires_at` продлевается при отмене — иначе отмена = ловушка на протухшей ссылке. Отдельного роута «перенос» НЕТ: перенос = cancel + повторный `/book`.
- **Календарь Б24:** добавлен `delete_calendar_event` (`calendar.event.delete`, params `{id, ownerId, type:'user'}` по симметрии с `event.add`). **FAIL-SOFT:** сбой Б24 отмену НЕ откатывает (иначе кандидат просто не придёт, а слот останется занятым), но пишет `b24_event_deleted: false` в audit и предупреждение в письмо рекрутёру. ⚠️ На живом портале НЕ пинено.
- **ICS:** `_build_ics` параметризован (`method`/`event_status`/`sequence`); **UID остаётся стабильным** (`{token}@glafira`) → почтовый клиент обновляет ТУ ЖЕ встречу, `METHOD:CANCEL` снимает её. ⚠️ Реальным Gmail/Outlook не проверено.
- **Дыра, которую закрыли попутно:** письмо-ПОДТВЕРЖДЕНИЕ брони давало только ссылку на видеовстречу, без ссылки на управление записью — кандидат, закрывший первое письмо, оставался без выхода. Теперь ссылка есть в обоих письмах.
- **v1.1.2** — формулировка «Рекрутёр уведомлён» → «получит уведомление»: Event/audit пишутся всегда, а письмо рекрутёру best-effort (нет email / не настроен SMTP → не уйдёт), утверждать свершившийся факт нельзя (§0).
- **v1.1.3** — починка вакуумного теста слотов (см. врезку про вакуумные тесты в §3). Полный прогон после эры: 1202 passed + 1 failed → после фикса **1203/1203**.

**Свежие фичи (1.1.4–1.1.6) — ЗАКАЗЧИК ЗАЯВКИ ИЗ СОТРУДНИКОВ ГЛАФИРЫ:** рекрутёр в форме заявки выбирает заказчика из заведённых пользователей вместо ручного ввода текстом — заявка ПРИВЯЗЫВАЕТСЯ к сотруднику (`author_user_id`), он видит её в «Моих заявках» и получает письма о смене этапа.
- **v1.1.4 (бек)** — `POST /requests` принимает необязательный `author_user_id` (только `via='manual'`). Привязать можно ТОЛЬКО **активного пользователя СВОЕЙ компании** (§2.3) — иначе рекрутёр арендатора А подсунул бы заявку юзеру арендатора Б через author-scoped список; не найден → fail-closed 400 с формулировкой, НЕ различающей «нет такого»/«чужая компания»/«заблокирован» (защита от зондирования). ФИО/должность берутся ИЗ ЗАПИСИ юзера — **сервер источник правды**, присланный клиентом текст игнорируется осознанно. Привязка пишется в `audit_log` (§2.2). `via` остаётся `'manual'` — вносит всё равно рекрутёр.
- **v1.1.5 (фронт)** — переключатель «Из Глафиры / Вручную» на идиоме `.req-fp-seg` + пикер с поиском (идиом `.avm-*` из AssignToVacancyModal, попап `.ctp-menu` из CandidateTagPicker), классы `req-up-*` скоуплены к `.req-fp-card`.
- **v1.1.6** — пост-фактум ревью (27 агентов, 10 находок, 5 выжили после адверсной верификации) + фиксы:
  - ⚠️ **Рассинхрон гейта роли — фронт vs бек (§0 «мёртвый контрол»).** Фронт гейтил по `role === 'hiring_manager'`, а бек — по `role not in ('admin','recruiter')` (`api/v1/requests.py:188`) и `_is_staff` (`services/hiring_request.py:48`). Роль **`manager`** попадала в рекрутёрскую ветку: видела пикер, грузила `/users`, выбирала сотрудника — сервер молча выбрасывал выбор (via='cabinet' не читает `author_*`, контакт затирался в None). На экране списка было хуже: кнопки «Ссылка на форму»/«Воронка» вели на `/settings`, откуда `RoleGuard(['admin','recruiter'])` **выбрасывал на /home**, тулбар панели давал 403, а `useRequestFormLink` слал обречённый запрос. Фикс: `isMgr` в ОБОИХ файлах (`NewRequestPage.tsx`, `RequestsPage.tsx`) = «не admin и не recruiter», зеркально беку, с комментарием «правишь — правь бек». Дефект был ПРЕДСУЩЕСТВУЮЩИЙ (до фичи так же терялся текстовый `author_name`) — фича лишь сделала его видимым.
  - **Заказчик по умолчанию — залогиненный пользователь** (требование заказчика): предвыбран из `authStore` (`UserMe` содержит `id`/`full_name`/`position`/`avatar_url` — второй запрос НЕ нужен), крестик сбрасывает, возврат «Вручную»→«Из Глафиры» ставит его снова. Локальный тип `AuthorOption` вместо `as`-каста (`UserMe` и `UserListItem` укладываются в него структурно).
  - **Тумблер уведомлений обещал больше, чем делал.** Подпись говорит «письмо при изменении статуса заявки», а `_notify_manager_stage_change` вызывался только в 4 местах — немыми оставались `link_vacancy_to_request` (work→`sourcing`, «В подборе» — момент, когда подбор реально начался, самый значимый для заказчика), `restore_request` (rejected→work) и `add_comment` при `question_moves_to_work` (new→work). Добавлены все три; ярлык — из `_FIXED_LABELS` (справочник был мёртвым кодом), в `link_vacancy_to_request` под условием `req.status != before_status` (у терминальной заявки вакансия привязывается, но статус не меняется — врать про «В подборе» нельзя). Задвоения нет: повторный `/move sourcing` упирается в ранний `if target == req.status: return`.
  - ⚠️ **Точка патча писем контринтуитивна:** `send_email` импортируется ЛОКАЛЬНО в теле `_notify_manager_stage_change` (`hiring_request.py:212`) → модульного имени `app.services.hiring_request.send_email` НЕ существует, патч по нему создал бы фантом-атрибут и **вечнозелёный тест**. Патчить `app.services.integrations.smtp.service.send_email`.
  - Фикс вёрстки: подпись «Заказчик» стала flex с сегмент-переключателем (~22px) против обычной подписи справа (~15px) → поля grid-строки вставали на разной высоте. Общая `min-height:24px` подписям строки, скоуплено модификатором `.req-fp-row-author` (глобальные `.req-fp-row2`/`.req-fp-field>span` делят другие строки формы).
- **Открытые хвосты эры (НЕ баги, решения за заказчиком):** (а) **самоуведомление** — если автор сам задаст первый вопрос по своей заявке, письмо о переходе в «В работе» придёт ему же (фильтр «не писать о собственном действии» — продуктовое решение, помечено комментарием `hiring_request.py:556-559`); (б) `link_vacancy_to_request` пишет в audit хардкод `before={"status":"work"}` (:597), хотя заявка могла быть в `new`/кастомном этапе — рядом теперь есть готовая `before_status`, замена в одну строку; (в) уведомления шлются **до `commit()`** (так во всех 7 вызовах) — письмо теоретически может уйти о переходе, который откатится; закрывается только выносом в after-commit hook сразу для всех.

**Свежие фичи (1.1.8) — «ОТПРАВИТЬ ОФФЕР» (письмо-оффер кандидату на этапе «Оффер»):** в карточке кандидата на этапе `offer` рекрутёр/админ шлёт кандидату брендированное письмо-оффер; тело генерит Глафира (LLM) «языком HR-оффера» из фактов вакансии, рекрутёр правит в попапе и отправляет на email. Приветствие («верх») и подпись («низ») настраиваются в общих настройках.
- **Триггер:** кнопка в тулбаре карточки видна СТРОГО при `application.stage === 'offer'` (этап удаляемый — нет этапа → нет кнопки, само собой) И только не-manager (`canEdit`). Серверный гейт дублирует: слать оффер можно только с этапа `offer` (иначе 400). Нет email у кандидата → кнопка disabled с пояснением (§0, не молча).
- **Бек:** `services/glafira/offer.py` (`generate_offer_body` + детерминированный `_fallback_offer_body` — LLM недоступна/сбой → осмысленный шаблон из фактов вакансии, НЕ 502, НЕ фейк; anti-hallucination guard: только факты, ДМС/бонусы/даты не выдумывать, инструкции из описания игнорировать; `call_text` per-company модель); `services/offer.py` (`send_offer` + `build_offer_preview` + `_effective_header_footer` + дефолты `DEFAULT_OFFER_HEADER/FOOTER`). Письмо ТОЛЬКО через `render_simple_email` (весь текст `html.escape`), отправка `send_email` ПЕРЕД записью `Message`(channel='email',direction='out') + `audit(action='send_offer')` — сбой SMTP → честная 400, фейкового «отправлено» нет. `resolve_company_display_name` (только `client_id`, MissingGreenlet). Роуты `POST /applications/{id}/offer/generate|send` (`applications.py`, RBAC: manager инлайн-403, hiring_manager отсечён `_deny_hm`; коммит в роуте). Event(type='offer') НЕ пишем (нет в CHECK) — видимость через Message в табе Чат + audit.
- **Настройки:** `GlafiraSettings.offer_email_header/offer_email_footer` (Text nullable, миграция `c4d5e6f7a8b9`, down_revision `b3c4d5e6f7a8`). Редактируются в Настройки → Шаблоны сообщений (рядом с «Текстом отказа»). ⚠️ Пустое поле фронт шлёт ПУСТОЙ СТРОКОЙ (не null): бек применяет через `is not None`, поэтому null молча игнорился бы (поле не очищалось), а `''` сохраняется и на сборке письма трактуется как «вернуть дефолт» (`_effective_header_footer`: `(x or "").strip() or DEFAULT`).
- **Фронт:** кнопка в `CandidateToolbar.tsx`; попап `OfferModal.tsx` (генерация при открытии, header/footer read-only вокруг редактируемого тела, ошибки из конверта ApiError); мутации `api/mutations/offer.ts`. openapi протух → локальные типы + as-cast.
- **Пост-фактум ревью (адверсное, 17 агентов):** 6 находок, все low/medium, 0 blocker/high; исправлены в v1.1.8: пробельное тело (field_validator strip→422), кнопка у manager (`&& canEdit`), очистка настроек (фронт шлёт `''`), +тесты сбоя SMTP/recruiter-200/hiring_manager-403. Изоляция/контракт/токены/экранирование — чисты по ревью.
- ⚠️ **Не в scope (за заказчиком):** оффер НЕ попадает в ленту «Все действия» (нужен Event.type='offer' с миграцией CHECK + рендер) — сейчас виден только в Чате + audit. Гейта согласия ПдН на оффер НЕТ (коммуникация по воронке, не верификация 152-ФЗ).

**Свежие фичи (1.1.13) — ФИКС пустого тела оффера (тест выявил на VPS):** `/offer/send` — multipart, и ПУСТОЕ значение формы `body=""` FastAPI трактует как ОТСУТСТВУЮЩЕЕ поле → `Form(...)` (required) даёт 422 «Field required» ДО ручной проверки; а пробельное `"   "` считается присланным → доходит до `if not body.strip()` → 400. Нестыковка (пусто=422, пробел=400). Фикс: `body: str = Form(default="")` — любое пустое (нет поля / `""` / пробелы) доходит до ручной проверки → единая честная **400** «Тело оффера не может быть пустым». Прод-риска не было (фронт всегда шлёт непустое, `if(!body.trim())return`); всплыло полным прогоном `test_send_offer_empty_body_400` на VPS (18/19 → фикс → 19/19). ⚠️ Урок: при multipart-эндпоинте `Form(...)` required + пустое значение = 422, не наша 400 — для единой бизнес-ошибки давать `Form(default="")` и проверять `strip()` вручную.

**Свежие фичи (1.1.12) — ОФФЕР В ЛЕНТЕ «ВСЕ ДЕЙСТВИЯ» + БЕЙДЖ «Отправлен ✓ дата»:** отправка оффера теперь фиксируется событием и виден статус отправки в тулбаре.
- **Event `type='offer'` в ленту:** `send_offer` пишет `Event(type='offer', actor_type='human', text="Отправлен оффер по вакансии «…»" (без PII), candidate_id, vacancy_id)` — появляется в табе «Все действия» карточки И на Главной. ⚠️ **МИГРАЦИЯ CHECK НЕ НУЖНА** — `'offer'` в `check_event_type` (`models/event.py`) с самой ПЕРВОЙ миграции (`824a6e6b9004`); фронт УЖЕ рендерил тип (`AllActionsTab` EVENT_MAPPING + `EventsFeed` EVENT_ICON) — ленты не трогали. (Закрыт хвост «оффер в Все действия».)
- **Бейдж «Отправлен ✓ ДД.ММ.ГГГГ»** рядом с кнопкой «Отправить оффер» (по образцу «ПдН ✓», класс `.cd-offer-sent`). Кнопка ОСТАЁТСЯ (повторная отправка — дата обновляется). Данные: новая колонка `applications.offer_sent_at` (TIMESTAMP tz nullable, миграция `e6f7a8b9c0d1` ← `c4d5e6f7a8b9`, одна голова; `send_offer` ставит `= now`). Отдаётся в `ApplicationRow.offer_sent_at` (SELECT + конструктор `services/application.py`; ⚠️ ВТОРОЙ конструктор `candidate.py::assign_candidate_to_vacancy` не передаёт поле → в схеме дефолт `= None`, свежая заявка = оффер не слался, НЕ «поле молча теряется»). ⚠️ `offer_sent_at` — обычная колонка, а `has_pdn` — вычисляемый subquery (не копировать её механику).
- **Порядок сохранён:** `offer_sent_at`/`Event`/`Message`/`audit` — ВСЁ после `send_email` (сбой SMTP → нет следа, тест `test_send_offer_smtp_failure_no_fake_sent` это охраняет). Кэш: `useSendOffer` инвалидирует `['vacancies']` (там ApplicationRow с offer_sent_at) + зеркало ПдН (`['applications']`/`['candidates',id]`/`['home','events']`).
- Тесты: усилены success (Event создан + offer_sent_at не None) и smtp-failure (Event нет + offer_sent_at None). Локально не прогнаны (рантайма нет).

**Свежие фичи (1.1.11) — ВЛОЖЕНИЕ ФАЙЛА В ОФФЕР:** в попапе «Отправить оффер» можно прикрепить ОДИН файл (PDF/Word/Excel/картинка/текст, ≤10 МБ) — он уходит вложением в письме кандидату. Без файла оффер работает как прежде.
- **Email-ядро (аддитивно, не задело остальные письма):** `_build_message`/`send_via_smtp`/`send_email` получили параметр `attachments: list[dict] | None = None` (элемент `{filename, content: bytes, maintype, subtype}`), цепляются через `EmailMessage.add_attachment` поверх `ics`. Все 8 вызывающих `send_email` работают как прежде (не передают attachments); передаёт только `offer.py`.
- **`/applications/{id}/offer/send` теперь multipart/form-data** (`body: str = Form`, `file: UploadFile | None = File(None)`) — раньше JSON. Файл читается/валидируется В РОУТЕ: кап 10 МБ + белый список расширений (`.pdf/.doc/.docx/.xls/.xlsx/.txt/.rtf/.png/.jpg/.jpeg`) → плохой файл честная **400**, письмо НЕ уходит; имя санитайзится (`_sanitize_attachment_filename`: basename без путей, срез control/newline = защита от инъекции заголовка + path-traversal, ≤150; EmailMessage вдобавок RFC2231-кодирует). 0 байт = «нет файла». MIME из `content_type` → фолбэк по расширению → `octet-stream`. Файл НЕ персистится (это письмо) — в `Message.body` пометка `📎 Вложение: {filename}` (след в Чате), в audit `has_attachment` (имя файла в audit НЕ кладём). Порядок «send_email → Message → audit» сохранён (сбой SMTP → нет фейкового «отправлено»).
- **Пустое/пробельное тело оффера → 400** (`if not body.strip()` в роуте; при multipart Pydantic-валидатор не применяется — прежние `_422`-тесты стали `_400`). Схема `OfferSendRequest` удалена (была только под JSON-роут).
- **Фронт:** `OfferModal` — скрытый `<input type=file accept=...>` + кнопка «Прикрепить файл»/чип (имя·размер·сброс, идиом `.req-up-picked`); `useSendOffer` шлёт `FormData` (`Content-Type: multipart/form-data`, идиом `useUploadDocument`; interceptor ставит только Authorization — boundary не перебивается). Иконка `paperclip`.
- **Тесты (+5, все send-тесты переведены на multipart `data=`/`files=`):** валидный файл (attachments с байтами/именем/MIME + пометка в Message), >10 МБ→400, `.exe`→400, path-traversal в имени → срез до basename, юнит-санитайз (control/newline). ⚠️ **Урок:** смена JSON→multipart на эндпоинте требует перевода ВСЕХ его тестов на `data=`/`files=` — иначе JSON-body против multipart даёт 422 (протухшие тесты). 1233 → 1238.
- ⚠️ Не в scope: сохранение файла в Документы кандидата (только email-вложение + след в Чате).

**Свежие фичи (1.1.10) — ФИКС «попап оффера висит»:** генерация тела оффера (`build_offer_preview`) — ИНТЕРАКТИВНЫЙ вызов, а `_GENERATE_TIMEOUT_SEC` был **50с** при `_HTTP_TIMEOUT.read=120с` в LLM-клиенте → при медленном/сбойном OpenRouter попап крутил спиннер до 50с (жалоба «висит постоянно»). Урезано до **15с** (`services/offer.py:48`): успешная генерация короткого оффера укладывается в секунды, иначе быстрый детерминированный фолбэк-шаблон (ключ строго per-company: нет ключа → мгновенный фолбэк без вызова; бесконечного зависания не было — `wait_for` не проглатывает `CancelledError`, `generate_offer_body` ловит `except Exception`). ⚠️ Если ждут AI-текст, а стабильно приходит шаблон — проверить per-company OpenRouter-ключ/модель в суперадминке (невалидный ключ = живой вызов падает → фолбэк). Фото-прокси `/public/photo` 404 (протухший подписанный URL hh) — отдельная косметика, к офферу не относится, аватар падает на инициалы.

**Свежие фичи (1.1.9) — ЭТАП «ОФФЕР» ЗАЩИЩЁН ОТ УДАЛЕНИЯ (надстройка над фичей оффера):** раз к этапу `offer` привязана отправка оффера (v1.1.8), удаляемый этап оставил бы фичу без триггера → конфликт. Теперь `offer` «прибит гвоздями», как `hired`.
- **Бек:** `'offer'` добавлен в `PROTECTED_STAGE_KEYS` (`core/stages.py`) → нельзя удалять/менять stage_key в вакансии, шаблонах воронки и воронке по умолчанию (3 точки CRUD). ⚠️ **Развязка со скорингом:** `PROTECTED_STAGE_KEYS` раньше служил и списком «куда НЕ авто-переводить» — тупое добавление `offer` молча запретило бы автоперевод в оффер (§0). Заведена отдельная `AUTO_MOVE_EXCLUDED_STAGE_KEYS` (= прежнее значение PROTECTED, БЕЗ `offer`); `resolve_auto_target_stage` использует ЕЁ → поведение автоперевода не изменилось, авто-перевод в «Оффер» остаётся допустимым (тест `test_auto_move_target_can_be_offer`).
- **Фронт:** удаление `offer` запрещено в ОБОИХ редакторах воронки (`VacancyFormPage` + `SettingsFunnel` = Настройки→Воронка), кнопка disabled с честной причиной «Этап «Оффер» нельзя удалить — к нему привязана отправка оффера». Оффер остаётся обычным СРЕДНИМ этапом (переименование/порядок разрешены, НЕ мислейблим в «Системный»): конфликт был именно про удаление stage_key, а не про label/порядок. `SettingsFunnel.saveStages` не шлёт delete оффера даже при рассинхроне.
- ⚠️ **Forward-only:** защита не форс-восстанавливает `offer` в вакансиях, где его уже удалили ДО v1.1.9 (там кнопка оффера просто не появится); миграции данных нет. Существующие воронки без оффера остаются без него.

**Текущая версия:** `frontend/src/lib/version.ts` (`APP_VERSION`) — на 2026-07-24 = **1.1.13**. Видна в Sidebar под «Глафира» (и в шапке суперадминки). **Bump на каждый значимый деплой.**

---

## 6. ДЕПЛОЙ И РЕЛИЗ-ЦИКЛ (VPS)

**Релиз = `git push origin main`.** GitHub Actions (`.github/workflows/deploy.yml`, appleboy/ssh-action) идёт по SSH на VPS `/var/www/glafira`:
1. `git pull --ff-only`;
2. rebuild `backend` — только если в диффе менялись пути `backend/`; rebuild `frontend` — только если `frontend/`;
3. `docker compose -f docker-compose.prod.yml up -d`;
4. `alembic upgrade head` — **ТОЛЬКО если в диффе есть файлы `backend/alembic/versions/`**.
5. пересборка+рестарт `superadmin` (`--profile superadmin build` + `up -d superadmin`) — **ТОЛЬКО если менялись `backend/superadmin/` И контейнер уже запущен** (иначе пропуск; первичная настройка/подъём — ручные). Суперадмин — отдельный сервис (profiles), обычный `up -d` его не трогает. ⚠️ Этот блок в `deploy.yml` правится через GitHub web UI: `.github/workflows/*` нельзя пушить токеном без scope `workflow`.

**CI тесты НЕ гоняет — только деплой.** `pytest`/`seed` запускаются вручную на VPS в контейнере backend.

**Кто релизит:** Claude ведёт **полный цикл сам** — бампнуть `APP_VERSION` → коммит в `main` (с `Co-Authored-By: Claude`) → `git push`. Не переспрашивать. (Коммитим прямо в `main` — так шёл весь конвейер.)

**⚠️ ВЕРСИЯ — ПЕРВЫМ ТОКЕНОМ ЗАГОЛОВКА КОММИТА, ВСЕГДА.** Формат: **`vX.Y.Z тип(scope): описание`** (напр. `v0.3.31 feat(stages): …`). В action/списках GitHub заголовок обрезается — версия в конце/теле НЕ видна, поэтому строго в начале subject. Пользователь требовал это многократно (см. память `claude-commits-and-bumps-version`). Применять к КАЖДОМУ коммиту, включая бэкенд-only и docs.

**Ручные docker-команды на VPS — ВСЕГДА `-f docker-compose.prod.yml`.** Голый `docker compose up -d` хватает дев-`docker-compose.yml` (другой том postgres `postgres_data` ≠ прод `glafira_pgdata`, открытые порты, авто-seed) — это ломает/подменяет прод. Автодеплой и любые ручные операции — только prod-файл.

**Рантайм-реальность этой среды:** локального бэкенд-окружения НЕТ (нет Docker CLI, нет venv/зависимостей, нет SSH к VPS). ⚠️ **И НЕ поднимать его — даже если технически можно** (проверено 2026-07-20 и прервано заказчиком: «дай команду на VPS прогони, а ты откати всё»). Соблазн реален: локальный Postgres 17 запущен, `conftest` уважает `TEST_DATABASE_URL`, база `glafira_test` существует, `pgvector` ставится из brew — я собрал окружение и получил откат. **Тесты гоняет заказчик на VPS; правильный ответ на «прогони тесты» — выдать команду.** (Если однажды всё же понадобится: на Python 3.14 нужен ЯВНЫЙ `pip install greenlet`, иначе ~960 ошибок `ValueError: the greenlet library is required`; и `pytest.ini:11` жёстко пишет `--junitxml=/app/storage/test-results.xml` — вне контейнера падает `OSError: Read-only file system: '/app'`, нужен override.) Значит:
- `pytest`/`seed`/`alembic`/`uvicorn` локально НЕ запускаются — писать код+тесты+миграцию, коммитить, пушить; прогон тестов — на VPS (`docker compose -f docker-compose.prod.yml run --rm backend pytest`). «pytest зелёный» НЕ утверждать как факт, если не прогнан.
- Фронтовый тулинг ЕСТЬ локально: `npx tsc --noEmit` и `npm run build` гонять обязательно перед коммитом.

**Инфра-инварианты VPS (.env НЕ в git):** `DATABASE_URL`, `JWT_SECRET`, `FERNET_KEY`, `OPENROUTER_API_KEY`, `GLAFIRA_MODEL`, `CORS_ORIGINS=https://glafira.dclouds.ru`, `SESSION_COOKIE_SECURE=True`. Верификация: `DADATA_API_KEY`+`DADATA_SECRET_KEY` (контакты), OSINT — `CLAUDE_TOKEN_DIR` (хост-папка с токен-файлом, монтируется в `/app/claude-auth`) + `CLAUDE_TOKEN_FILE=/app/claude-auth/.claude_token.json` (или `CLAUDE_CODE_OAUTH_TOKEN`), `GLAFIRA_OSINT_MODEL=opus`, `GLAFIRA_OSINT_TIMEOUT=120`. Звонки Mango: расшифровка идёт через существующий `OPENROUTER_API_KEY` (модель `GLAFIRA_TRANSCRIBE_MODEL`, дефолт `google/gemini-2.5-pro` — отдельного Gemini-ключа НЕ нужно); ключи самого Mango (`vpbx_api_key`/`vpbx_api_salt`) — per-company в БД (Fernet), НЕ в env. Telegram: `TG_SEND_LOG_PATH` (дефолт `/app/storage/tg_send.log` — журнал резолва пира и исхода отправки: OK / NO_ACCOUNT / LIMITED, v0.9.214). Фронт build-time: `VITE_API_BASE_URL=https://glafira.dclouds.ru/api/v1`.
- **Права вебхука Битрикс24 — `user` + `calendar` + `im`** (раньше хватало `user`; `calendar` для занятости/события интервью, `im` для авто-генерации видеокомнаты). Нет scope → «higher privileges than provided by the webhook token». Ключи Б24 — per-company в БД (Fernet), НЕ в env.
- **Кроны на VPS заказчик добавляет РУКАМИ** (в репозитории нигде не декларированы — `grep` по `*.sh`/`*.yml` пуст): `app/jobs/score_pending.py` (авто-скоринг **И рассылка ссылок на интервью П.5** — без него П.5 молча не работает), `app/jobs/poll_hh_responses.py`, `app/jobs/poll_telegram_messages.py` (flock, раз в 3 мин), `app/jobs/regenerate_employee_summaries.py`.
- Порты бек/фронт — на `127.0.0.1` (наружу только через общий nginx). БД-контейнер без публикации порта.
- CORS в `main.py` читает из env (НЕ хардкод localhost).
- `X-Forwarded-Proto $scheme` в nginx обязателен (Secure-cookie, иначе авторизация слетает).
- nginx **общий** — добавлять только свой server-блок, `nginx -t` перед reload, чужие не трогать.
- При проверке фронта в браузере — **hard-reload (Ctrl+Shift+R)**, CSS/шрифты кэшируются.
- Прод-БД персистентна (named volume); сид НЕ запускается автодеплоем — только вручную. Перед прод-сидом — бэкап БД.

---

## 7. КОМАНДЫ

> Бэкенд-команды выполняются на VPS в контейнере backend (локального рантайма нет). Фронтовые — локально.

**Бэкенд (на VPS, `docker compose -f docker-compose.prod.yml run --rm backend ...`):**
```bash
pytest                                    # тесты (offline, LLM-ключ пуст)
alembic upgrade head                      # миграции
alembic revision --autogenerate -m "..."  # новая миграция
python -m app.seed                        # базовый seed
python -m app.seed_demo                   # демо-данные (сносит/пересоздаёт demo)
```

**Фронтенд (локально):**
```bash
cd frontend
npm run dev          # локальная разработка
npm run build        # production-сборка (tsc -b && vite build)
npx tsc --noEmit     # проверка типов
npm run types:gen    # регенерация типов из ../contracts/openapi.json -> src/api/types.ts
```

**Контракт:** после изменения схем бека — регенерировать `contracts/openapi.json` (нужен живой бэк), затем `npm run types:gen`. Пока сервера нет — новые тела через локальный тип + `as`-cast, и пометить в отчёте, что openapi не регенерён.

---

## 8. ЧЕК-ЛИСТ ПЕРЕД КОММИТОМ

- Фронт: `tsc --noEmit` + `npm run build` — **чистые** (прогнать обязательно). Бек: тесты написаны (прогон на VPS, если локально нечем — не утверждать «зелёный» голословно).
- `grep` по новым/изменённым местам: нет `console.log`, нет `alert()`, нет mock/хардкод-фейков, нет выдуманных токенов в `tokens.css`, нет несуществующих эндпоинтов в тестах.
- Изменяющие действия → `audit_log`; новые записи → `company_id`.
- Контракт: openapi регенерён, если есть живой сервер; иначе локальный тип + cast + явная пометка.
- Обратная совместимость: seed (базовый + demo) не сломан, существующие данные/воронки целы.
- При пересборке экрана: функционал из чек-листа жив (проверить руками/диффом, НЕ по отчёту агента); `.submenu-search` не тронут; CSS scoped; сверено с `project/` (включая новые элементы — ближайший идиом, не своё).
- Bump `APP_VERSION` (`frontend/src/lib/version.ts`).
- **Заголовок коммита начинается с `vX.Y.Z`** (`vX.Y.Z тип(scope): описание`) — первым токеном, ВСЕГДА.
- Релиз: коммит в `main` + `git push` (= автодеплой). Затем ручная проверка в браузере на VPS (hard-reload): визуал + функционал.
